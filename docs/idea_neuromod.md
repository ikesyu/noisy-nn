# 神経修飾場の研究ノート — 修飾物質を「何に」結びつけるか

**この文書の役割**: NNN のノイズ場 $\mathcal P=\{(\sigma_k,h_k)\}$ を神経修飾場のモデルとして扱う
研究線の**単一の文書**。主張の骨格と限定（第 1 節）、修飾物質を計算論的に何に結びつけるのが
生物規範的かの検討（第 2〜5 節）、それを支える実測（第 6 節）、そこから導かれるデモ案（第 7 節）を置く。
投稿想定は Frontiers in Computational Neuroscience の Research Topic
「Computational Models of Neuromodulation」。記事種別は **Hypothesis & Theory**（次点：Methods /
Technology & Code）。Original Research は避ける。主張は機構仮説とその最小実証であり、
新規の神経データ解析ではないため。

> **投稿情報（※メモ由来・要再確認）**: IF 3.3、締切 2026-09-11、編集者に Tomoki Kurikawa
> （公立はこだて未来大学）ほか。投稿前に Research Topic ページで最新の締切・スコープ・
> 記事種別を確認すること。

**正典**: 記号・定義は `docs/idea_core.md` に従う。特に効くのは §2.6（SR は sample 水準にしか
存在しない）、§3.3–3.5（交差率 $\nu$、$\sigma=0$ リーク、kill 三点操作）、§4（ゲージ対称性）。

**関連文書**: `idea_consolidation.md`（$\rho$ による資源制御、圧縮・逐次格納）、
`idea_rl.md` §25（探索温度の内部化）、`draft_nce.md`（forward-only 学習則）、
`idea_coding.md`（同じ SR 論法の時間軸版）。
**実装の配置**: 標準問題の共通部は **`tmp/neuromod/`** パッケージ（`world` 問題設定・感覚符号化・
行動目標・閉ループ駆動 / `fields` ノイズ場と交差率による参加度 / `protocol` 学習と採点 /
`viz` 描画・アニメ・保存）。個別のチャレンジは `tmp/` 直下のスクリプトで、共通部を import して
自分の問いだけを変える。現存するのは `tmp/neuromod_behavior_modes.py`（行動デモの参照ドライバ、L1）、
`tmp/neuromod_sr_curve.py`（SR 曲線、L3）、`tmp/neuromod_baseline_shift.py`（ベースライン依存の
効果反転、§7.2）、`tmp/neuromod_lesion.py`（損傷・重なり、L2 / §7.3）。

> `tmp/neuromod/` にはこの問題に属するものだけを置く。学習則は `nnn/`、RL・コンソリデーション・
> リザバーは各々のパッケージにあり、神経修飾の取り組みはそれらにほとんど依存しないので、
> それらの機構をここに持ち込まないこと。

**改稿**: 2026-08-02。旧稿（SR 図の作成手順）と、別稿にあった投稿戦略
（主張の限定・L1–L4・批判への応答・損傷実験の定義）を本稿に統合し、生物対応づけの議論を
主軸に据え直した。本稿がこの研究線の単一の文書である。SR レシピは付録 A・B に圧縮して残してある。

---

## 0. この文書の要点

1. **現デモの結びつけ方（修飾物質 → どの対象カテゴリに向かうか）は、生物学的な裏付けが最も薄い部類である。**
   モノアミン系に「食物モジュレータ」「シェルターモジュレータ」の対応物は無い（§4）。
2. **しかもこの結びつけは「結局 one-hot の鍵では？」という批判を自ら誘発する。**
   SR（内点最適）を主張の芯に据えるなら、修飾物質は**スカラーの計算メタパラメータ**に結びつけ、
   行動の違いはその帰結として出すほうが、生物側の逆 U 字文献と主張が直結する（§2, §3）。
3. **理論側から結びつけ先が制約される。** $\sigma$ の絶対値はゲージ依存量なので「修飾物質 $=\sigma$」は
   単独では意味を持たない。結びつけるべきは $\rho$（動員ダイヤル）または $\nu$（交差率）、
   実質的には「閾値までの距離を膜電位ゆらぎの標準偏差で測った量」である（§5）。
4. **L1・L2・L3 は実験で揃った。L4 は半分だけ成立した。**
   場が離散スイッチではなく単調・連続な制御軸であることは決定的に示せたが、
   補間した場が生む中間行動は学習済み端点より約 7 倍不正確で、素朴な出力混合にも劣る
   （§7.11）。**原稿では L4 の質的部分だけを主張し、量的部分は主張しない。**

### 到達点（2026-08-04 時点）

| 段 | 結果 | 図・データ |
|---|---|---|
| **L1** アドレッシング | 共有重み + 3 場 → 3 行動（実装済み） | `neuromod_pure_panels.png` |
| **L2** 多重化 | 共有ユニットの損傷が両行動を同時に劣化させる（3 seed）。**$\alpha_\text{mix}>0$ が条件**（§7.3） | `lesion_bars.png`, `lesion.csv` |
| **L3** 資源性 | 学習水準スイープで内点最適 $\sigma^*\approx0.8$–$1.2$、平均場では消える（§6）。さらに**同じ用量がベースライン次第で逆向きに効き、反転点が用量とともに移動する**（3 seed、§7.2） | `baseline_shift_s3_curve.png`, `baseline_shift_analytic_*` |
| **L4** 連続制御 | **質的部分は成立**: $\hat\lambda$ は Spearman 1.000・21 点すべて相異なり、離散スイッチを否定（3 seed、§7.11）。**量的部分は不成立**: 内点誤差が端点の約 7 倍で、素朴な出力混合にも 2–4 倍負ける | `interp_sample_mix06.png`, `interp_sample_mix06.csv` |
| §7.1 行動レベル SR | 濃度スライダー動画は完成。**厳密版は示唆的だが未確立**（多 seed が必要、§7.1） | `behavior_sr_slider.gif`, `behavior_sr_train*.csv` |

批判 (a)〜(e)（§1.3）のうち、実験を要する (b)(c)(e) はすべて裏付けが付いた。残るのは
(d) の文献整備（§8(a)）である。

### 標準ベンチマーク化（2026-08-08）

デモを標準ベンチマークとして固めた。形式的定義・標準パラメータ・実装規約は**付録 C** に
一元管理する（他所に仕様書を作らない）。要点:

- **sample 水準が全ドライバの既定**（SR は sample にしか存在しない、§6）。GPU 対応により
  40k ステップが約 1.5 分（CPU 約 31 分）。
- **調停は概日時計のみ**: 採餌⇔帰巣は満腹ではなく自走時計で切り替わる（付録 C.8）。
- **行動アーティファクト 3 種（脅威接触・壁張り付き・無意味な停止）を指標化して排除**
  （付録 C.8, C.9）。停止の原因は速度ベクトル低域通過の相殺で、速さ／方位分離積分で解消。
- **配置・食再生・概日周期は閉ループ実測で選定**（付録 C.2, C.9 の参照挙動）。
- **既定値は `world.LoopParams` に一元化**。ドライバ argparse は LoopParams から既定値を
  読み、`viz.animate` は LoopParams を直接受け取る。感覚の既定は sector 符号
  （旧 vector は再現用にレガシースクリプト側で明示ピン留め）。
- **レガシー経路を削除**（満腹則 drive、vector 積分、cruise 表示、random 配置）。
  ベンチマーク自体は新規性ではないため、主張の検証に必要な最小構成だけを残す
  （付録 C.0 の導出）。
- **文献上の骨格を「三つの鎖」として §1.1a に整理**: 行動↔修飾物質（STG で確立）→
  修飾物質↔ノイズ場（背景入力・LMAN・容積伝達）→ ノイズの効能＝SR（逆 U 字は導出される）。

---

## 1. 主張と論理の骨格

### 1.1 中心的主張と 4 段の論理

> **神経修飾物質様のノイズ場は、単一の重み集合に多重化された複数の方策を選択的にアドレスする。
> ノイズはシンボリックな「鍵（look-up key）」ではなく、最適強度をもつ機能的資源（確率共鳴）である。**

論理は「ノイズ場が方策を選ぶ」→「その選択は鍵ではなく資源である」→「ゆえに神経修飾の計算モデル
として意味がある」の順に閉じる。以後この 4 段を **L1–L4** と呼ぶ。

| 段 | 主張 | 根拠 | 状態 |
|---|---|---|---|
| **L1. アドレッシング** | 重みを一切変えずに、ノイズ場だけで異なる行動が選択される | 行動デモ：1 つの共有重み集合 + 3 つのノイズ場 → 3 つの識別可能な行動ベクトル場 | **実装済み** |
| **L2. 多重化（分割ではない）** | 各行動は互いに重なりうるサブネットに蓄えられており、排他的な区画への分割ではない | サブネット重なり（Jaccard、$S_i=\{k\mid\nu_k>0\}$）と共有ユニットの損傷実験（kill 三点操作） | **実装済み（§7.3、$\alpha_\text{mix}>0$ で成立）** |
| **L3. 資源性（鍵ではない）** | 動員されたサブネットの性能はノイズ強度に対して**内点最適**を持つ。鍵に最適強度は存在しない | SR 曲線：`--sweep train --model sample` で内点最適 $\sigma^*\approx0.8$–$1.2$（$h=0.2$ 大域固定の下で。§6） | **実装済み・主要結果** |
| **L4. 連続制御** | ノイズ場は離散スイッチではなく、単調で連続な制御軸である | 場の補間を含意混合率 $\hat\lambda$ で定量化（§7.11） | **質的部分は実装済み（§7.11）。「中間行動が学習済みと同等の精度で出る」は主張しない** |

**なぜ L3 が科学的な芯なのか。** L1 だけでは「ノイズ場は one-hot なスイッチ ＝ 記号的な鍵に過ぎない」
という解釈を排除できない。鍵ならば、閾値を超える限りどんな強度でも同じ行動が選ばれるはずで、
**最適強度は存在しない**。したがって内点最適の存在は、この対抗仮説に対する直接の反証になる。
ノイズは「どれを選ぶか」を運ぶだけでなく、「選ばれた計算を成立させる」ために必要な量として振る舞っている。

### 1.1a 三つの鎖 — 行動 → 修飾物質 → ノイズ → SR（2026-08-08 整理）

L1–L4 が実験上の骨格だとすれば、これは**文献上の骨格**である。取組のロジックは独立な
3 つの文献群を 1 本の機構で貫くことで立つ。各鎖の詳細は既存節に委ね、ここでは鎖の
構造だけを記す。

**鎖 1（行動 ↔ 修飾物質）— 確立事実。** 行動状態は秒〜分で切り替わるが結線はその速度で
変わらない。切り替えの実体が修飾物質であることは STG で確立され（同一回路が修飾物質
しだいで質的に異なるリズムを出す; §2.6）、探索⇔利用（LC-NE、§2.4）・採餌モード
（C. elegans、§3.5）・空腹による行動再構成（AgRP、§4）・脅威切迫度（Mobbs、§4）の
各軸で個別に実証されている。計算論的定式化は「修飾物質＝スカラーのメタパラメータ」
（Doya、§2.1）。**未解決なのは再構成の計算機構**であり、それが鎖 2 の問い。

**鎖 2（修飾物質 ↔ ノイズ場）— 三段の論拠。**
(a) ノイズの生理学的実体は背景シナプス入力で、その量がゲインを乗算的に変え
（Chance 2002）、修飾物質が制御する（§3.3, §3.4）。「σ を変える」は実在の操作。
(b) ノイズを**能動的に注入し文脈で制御する専用回路**が実在する（LMAN、§3.1）。
(c) 容積伝達（§2.7）は実際に**空間濃度勾配**を作る（放出部位周囲の影響圏、
神経支配密度の地形）。モデルの分離形 $\sigma_k=\sum_c w_c(t)\,s_k^c$ は
**準定常近似**として読む: 局所平衡化（拡散+取り込み、ms〜100ms）が放出トーンの
変化（秒〜分）より速いとき、濃度場は「トーン $w_c(t)$ × 定常プロファイル
$\text{profile}^c(x)$」に分離する。バンプ＝定常グリーン関数、$w$ の低域通過＝
トーンの蓄積・除去の速度論。受容体分布は実効場を整形する第 2 因子
$s_k^c=\text{profile}^c(x_k)\, g_k^c$（§1.1b ③）。
理論側の補正: σ 単独はゲージ依存で無意味。生物のつまみも 2 つ（興奮性 h / ゆらぎ σ）で、
効くのは比＝「閾値までの距離をゆらぎの標準偏差で測った量」（§5）。
固有の一手: 交差活性は σ→0 で無条件に沈黙するから、**ノイズ供給の空間パターンが
そのまま参加（動員）を決める**（§2.6a 筋 1）。これが鎖 1 の「機構は何か」への答。

**鎖 3（ノイズの効能 ↔ SR）— 逆 U 字は必然。** 閾値系では閾値下信号の伝達が中間ノイズで
最大になる（受容器→ヒト知覚→行動レベルまで実証階層あり; §3.2。行動レベルの直接先例は
パドルフィッシュの摂食 SR）。行動薬理側の逆 U 字（Yerkes-Dodson、DA-D1、瞳孔径 vs 検出;
§2.8）がその生体対応物。本取組の固有貢献は閉じ方にある:
**主張（本文）**: 訓練が各ユニットに与えるノイズ水準を {0, σ₀} の 2 値に制限しても、
未訓練の連続軸の上に滑らかな単峰カーブ（沈黙崩壊・内点最適・飽和劣化）と、場の
補間に対する法則的な応答（$\hat\lambda\approx w$、Spearman 1.000）が現れる
（§7.13 実験 8）。$\sigma{=}0$ の沈黙は重みに依らない解析的事実。閉ループの行動版は
実験 1（5 seed）。これが「ノイズ場は記号的な鍵ではなく、動作域を持つ機能的資源」の
実測形であり、曲線の**形**は訓練分布からは説明できない。

**補足（防御; 本文の芯に含めない）**: 単峰性の自明性監査——山の位置は適応が決め
（実験 3）、学習水準を振ると平坦（実験 2）。「訓練点から離せば悪いのは当然」への
回答はここに依る。**別論文候補**: 耐性・恒常性適応の全スレッド（実験 4–7）。

> **一文版（主ロジック 4 節、2026-08-09 確定）**:
> P1 行動のバリエーションは、同一ネットワーク上の神経修飾物質の**濃度分布**から
> 生まれる（STG で確立; デモ: 1 つの重みから 3 行動の多重化）。
> P2 その濃度分布は、準定常近似と受容体感受性を介して、集団上の**ノイズ場**として
> 妥当に表現できる（背景入力・LMAN・容積伝達）。
> P3 ノイズ場をパラメータとする NNN は、濃度軸に対して**SR 調の単峰カーブ**を示す
> （低濃度で沈黙崩壊——σ=0 は重みに依らず不可能、内点最適、飽和劣化; §7.13 実験 1）。
> C ゆえに **NNN は神経修飾物質を考えるための数理モデルの一つになりえる**。
> ベンチマーク（付録 C.0）はこの検証の最小構成として導出される。
>
> 耐性・恒常性適応（§7.13 実験 4–7）と単峰性の自明性監査（実験 2–3）は**主ロジックに
> 含めない**——前者は別論文候補、後者は査読防御用の補足（下記の位置づけ表を参照）。

**弱点（§0・§4 で自認済み）**: モノアミンに行動カテゴリのラベルは無い。最も近い実体は
視床下部ペプチド系（AgRP・オレキシン）。ゆえに場を行動ラベルに直接結びつけず、
内的 3 軸（時計・空腹・脅威）が場の混合 $w$ を決め行動は動員の帰結として出る、という
**因果の向き**を守ることが構造的防御になる（付録 C.8）。

### 1.1b 読み替えの台帳 — こじつけを生む箇所の事前指定（2026-08-08）

鎖（§1.1a）の中で生物→モデルの読み替えが起きるのは 6 箇所。**射影を恒等として
語った瞬間にこじつけになる**ので、各々の身分と「主張しないこと」を固定しておく。

| # | 読み替え | 身分 | 注意 |
|---|---|---|---|
| ① | σ（加法 iid ガウス）↔ シナプスノイズ | 標準的近似（拡散近似） | Chance 2002 は**分流性**のゲイン変調。「σ が操作可能な実在量」の証拠として引き、「我々の σ＝Chance の操作」とは書かない。加法ノイズの閾値平滑化の錨は Anderson et al. (2000) |
| ② | 交差活性 ↔ 単一ニューロン | **機能的等価物**（§2.6a） | 最重要。ただし Gidon et al. (2020) により「単一ニューロンに XOR とバンプ型 I/O の先例」は存在する（下記）。無いのは**ノイズ交差統計という機構**の対応 |
| ③ | 仮想シート ↔ 容積伝達の空間場 | **準定常濃度場**（主）＋受容体分布（従） | 主: バンプ＝放出源幾何が固定のときの定常濃度プロファイル、$w_c(t)$＝放出トーン。分離形は「局所平衡化（ms〜100ms・µm）≪ トーン変化（秒〜分）」の準定常近似で、遅い $w$ 動態＝濃度の蓄積・除去。神経支配地形（層別プロファイル、ストリオソーム/マトリックス）が「1 集団上に重なる複数プロファイル」の実在根拠。ペプチド系（本モデルの 3 軸の対応先）は分解が遅く長距離なので近似が通りやすい。従: 受容体感受性 $g_k^c$ が実効場を整形（$s_k^c=\text{profile}^c\,g_k^c$）。幾何が攻撃された場合の後衛が D1/D2（空間的に混在した 2 集団が同一濃度に逆応答＝幾何なしでもパターンは作れる存在証明） |
| ④ | 3 基底場 ↔ 3 修飾系 | 状態系への対応（§4） | 「food field」でなく**行動状態の動員パターン**と呼ぶ。状態特異的な視床下部ペプチド系は実在、刺激カテゴリのラベルは実在しない |
| ⑤ | McGinley ↔ SR 曲線 | **引用の役割を限定** | 膜電位ゆらぎは**中間覚醒で最小**なので「覚醒∝ノイズ」と読むと SR と逆向きに刺さる。逆 U 字の in vivo 実在の証拠としてのみ引く。SR の実証はザリガニ〜パドルフィッシュ系列に担わせる（§2.8 修正済み） |
| ⑥ | 濃度スイープ ↔ 用量反応 | ゲージ併記で防御済み（§6） | 「Yerkes-Dodson を説明」でなく「逆 U 字が導出される最小機構」。説明の一意性は主張しない |

**②の補足（2026-08-08）**: Gidon et al. (2020, Science) はヒト L2/3 錐体細胞の樹状突起
Ca²⁺ スパイク（dCaAP）が**閾値付近で振幅最大・入力増で減少**する非単調 I/O を持ち、
これにより**単一ニューロンが XOR を計算できる**ことを示した。交差活性 $2Q(1-Q)$ の
バンプ形状と XOR という論理は、単一素子レベルの先例を得たことになる。ただし機構は
異なる（決定論的な樹状突起電位 vs ノイズ交差統計）ので、身分は「対応物ゼロ」から
「**形と論理に先例あり・機構は未対応**」への格上げに留める。1 次モーメント側
（$\lambda=0$、閾値活性 $Q$）は確率的発火率モデルそのものであり生物対応は最も安全
（§2.6a）——ただし σ→0 で沈黙しないため、動員ゲートの物語には $h=h_0/\sigma$ の
2 変数協調が必要になる、という交換関係は変わらない。

### 1.1c アブストラクト草案（2026-08-08 確定版）

§1.1a の鎖と §1.1b の語彙規律に載せた圧縮。確立済みの主張のみ（行動レベル SR の
厳密版と L4 量的部分は含めていない。前者が §8 の実験で立てば一文追加できる）。

> Neuromodulators reconfigure anatomically fixed circuits into qualitatively
> different functional networks, a fact established in the crustacean
> stomatogastric ganglion, yet the computational mechanism of this
> reconfiguration remains unsettled. We propose spatial control of neuronal
> noise as a minimal mechanism. Physiologically, the intensity of membrane
> potential fluctuations is set by background synaptic input and is itself
> under neuromodulatory control. Volume transmission gives each modulator a
> spatially graded concentration profile over the population; because local
> diffusion and uptake equilibrate much faster than release tone varies, the
> concentration field factorizes into a slow tone multiplying a fixed spatial
> profile. We abstract the resulting pattern of locally induced fluctuation
> intensities as a noise field. In a network whose units are active only when
> noise drives threshold crossings, a noise field recruits a subnetwork from
> one fixed set of weights; units where the field is silent do not
> participate. We show that a single network, trained once, expresses three
> ecological behaviours, foraging, avoidance, and sheltering, as a simple
> internal dynamics (a circadian clock, hunger, and threat proximity) mixes
> three overlapping fields without any change of weights. The behaviours are
> multiplexed rather than partitioned, and the field acts as a continuous
> control axis. Performance exhibits an interior optimum in field intensity
> that vanishes in the mean-field limit: the noise is a functional resource
> with an optimal dose, not a symbolic key, and the inverted-U dose-response
> characteristic of neuromodulator pharmacology is derived from threshold
> physics rather than assumed.

文の対応（4 節構成）: P1＝STG→機構の問い＋3 行動・重み不変・多重化（鎖 1、L1/L2、
C.0）/ P2＝背景入力・準定常濃度場・受容体感受性（鎖 2、台帳①③）/ P3＝単峰カーブ・
沈黙崩壊・$\sigma{=}0$ の不可能性（鎖 3、§7.13 実験 1）/ C＝数理モデルの候補。
耐性（実験 4–7）はアブストに**含めない**（別論文候補）。

### 1.2 主張しないこと（重要な戦略判断）

- **行動の創発は主張しない。** デモの 3 行動（Foraging / Avoidance / Sheltering）は教師付き
  ベクトル場回帰で設計的に与えたものであり、「創発した」と述べた瞬間に「行動は設計されている」
  という批判が正当になる。創発は今後の課題として明示的に繰り延べる。
- **神経修飾物質の生理学的モデルであるとは主張しない。** 主張するのは「ゲイン／興奮性の修飾によって
  機能サブネットが選択される」という**計算原理の対応**であり、特定の伝達物質の薬理学的再現ではない。
- **中間濃度で「正しい」中間行動が出るとは主張しない。**（2026-08-04 追加）場が単調・連続な
  制御軸であることは §7.11 で示せたが、補間した場の下での行動は学習済み端点より約 7 倍不正確で、
  学習済み出力を線形に混ぜるだけの参照にも 2–4 倍負ける。**連続性は主張し、精度は主張しない。**

この 2 つを先に手放すことで、想定される 2 大批判が安価に回避でき、しかも残る主張
（アドレッシング + SR）は現有の実験だけで支えられる。

### 1.3 想定される批判と応答

| 批判 | 応答 | 必要な結果 |
|---|---|---|
| **(a) 行動は設計されたものであり、創発していない** | そのとおりであり、創発は主張しない。主張は「設計された複数方策が単一重み集合に多重化され、ノイズ場でアドレスされる」ことにある | なし（§1.2 の限定で回避） |
| **(b) ノイズ場は単なる one-hot の鍵ではないか** | 鍵に最適強度は存在しない。内点最適（SR）の存在が直接の反証。さらに**同じ介入がベースライン次第で逆向きに効き**、しかも**反転点が用量とともに移動する**（§7.2、3 seed で確認） | **L3（済）＋ §7.2（済）** |
| **(c) 単に区画分割されたサブネットのルックアップでは** | 場は重なりを持ち、共有ユニットの損傷は複数行動を同時に劣化させる（区画分割なら独立に壊れるはず） | **L2（済、§7.3）**。ただし $\alpha$ が近似 one-hot だと多重化は要請されないので、図は $\alpha_\text{mix}>0$ で作る |
| **(d) 神経修飾のモデルとしてのスコープ適合性** | 記事種別を Hypothesis & Theory とし、§2–§3 の対応表と引用で計算原理レベルの主張であることを明示 | Introduction の文献整備（§8(a)） |
| **(e) 決定論モデルでも同じことが言えるのでは** | 平均場（analytic）では SR が消え、低 $\sigma$ 端最適になる。閾値つきサンプル機構でのみ内点最適が現れる | **済**（§6 の analytic 対照） |

### 1.4 現デモの立ち位置と問題

現デモ `tmp/neuromod_behavior_modes.py` は、`ALPHA_STATES` を近似 one-hot に取り、
3 つのノイズ場が食物 / 脅威 / シェルターの各 drive に一対一で対応する構成になっている。
これは L1 を最短で見せるには良いが、二つの問題を抱えている。

- **生物学的な対応物が無い。** モノアミン（DA, NE, 5-HT, ACh）は「どの対象に向かうか」を
  labeled line で指定してはいない。指定に近い働きをするのは視床下部のペプチド性状態系であって、
  モノアミンではない（§4）。
- **主張の芯（L3）と噛み合わない。** 近似 one-hot の $\alpha$ の下では、場は事実上ルックアップキーとして
  働く。§1.3 の批判 (b) を、デモ自身が呼び込む形になっている。

したがって本稿の問いは「ノイズ場を神経修飾場と呼ぶとき、**修飾物質のどの計算的役割**に
対応づければ、L1 と L3 が同じデモの中で両立するか」である。

---

## 2. A. 生物規範的な結びつけの候補

### 2.1 計算論の背骨：メタパラメータとしての修飾物質

現在の標準的な立て方は **Doya (2002)** の metalearning 枠組みである。
DA = TD 誤差、ACh = 学習率、NE = 逆温度（探索）、5-HT = 割引率、という対応を提案しており、
「修飾物質 = 学習/決定のメタパラメータ」という見方が以後の基準になっている（Doya 2008 も同様）。

**本稿にとっての含意**: 修飾物質はまず**スカラー**であり、行動のラベルではない。
NNN で言えば、$\rho$ という単一のダイヤルに対応づけるのが自然であって、
3 本の独立な場に対応づけるのは筋が悪い。

### 2.2 ゲイン・SNR 変調（最も古典的で最も安全）

**Servan-Schreiber, Printz & Cohen (1990)** はカテコールアミンを活性化関数のゲイン変化として
定式化し、それを**そのまま SNR の変化として**扱い、逆 U 字を導いた。
一般論としては Salinas & Thier (2000)、Ferguson & Cardin (2020)。

**本稿にとっての含意**: NNN の実効ゲイン $\gamma_k=\lVert a_k\rVert/\sigma_k$（正典 §1.1）は
この量そのものである。対応は比喩ではなく形式的で、しかも $\gamma_k$ はゲージ不変なので
§5 の制約とも整合する。**引用として最も安全な一本**。

### 2.3 不確実性

**Yu & Dayan (2005)**: ACh が予期された不確実性、NE が予期されない不確実性を運ぶ。

### 2.4 探索 ↔ 利用（逆温度）

**Aston-Jones & Cohen (2005)** の adaptive gain theory。LC の tonic 活動と課題成績の間に
**逆 U 字**があり、tonic 高＝探索的、phasic ＝利用的という整理。

**本稿にとっての含意**: `idea_rl.md` §25 の「内部化された探索温度」がこれそのものである。
外付けの $\sigma_e$ を消して、方策自身の $T$ サンプルの広がりを探索温度にした構成は、
「修飾物質が逆温度を設定する」という命題の直接の実装になっている。ただしデモの順序としては
RL に踏み込む前に §7.1–7.3 を先に置く（§7.8）。

### 2.5 符号化 vs 想起の経路切り替え

**Hasselmo (2006; 1999)**: ACh 高で求心性入力を通し反回性結合を抑える、低で逆になる。
文字通り「同じ解剖構造の中でどの経路が計算に参加するかを修飾物質が決める」話であり、
**ノイズ場＝動員という主張の最も近い生理学的対応物**である。

**本稿にとっての含意**: `idea_consolidation.md` の $\rho$ ダイヤル（$\sigma_k=\rho_k\sigma_0$,
$h_k=h_0/\rho_k$）がそのまま「ACh 高＝新規学習に動員 / 低＝既存機能を保持」に読める。
ただし本稿でデモ化すると資源制御の話と主張が混ざる（§7.7）。

### 2.6 同一回路・複数出力（デモの最強の先行例）

**Marder (2012)**、**Marder & Bucher (2007)**、**Nusbaum & Beenhakker (2002)**。
甲殻類の胃腸神経節（STG）では、解剖学的に固定された一つの回路が、修飾物質しだいで
質的に異なる複数のリズムを出す。「重みは不変、場だけで行動が変わる」という L1 の主張の
生物学的先例としてこれ以上のものはない。**Bargmann (2012)** "Beyond the connectome" は
同じ論点を一般化しており、Introduction の掴みに使える。

関連して **Edelman & Gally (2001)** の degeneracy、**Marder & Taylor (2011)** の
「同じ振る舞いを複数のパラメータ集合が実現する」議論は、L2（多重化・重なり）の生物側の
受け皿になる（§7.3）。

> [new idea — 修飾物質が「読むモーメントの次数」を切り替える（`idea_core.md` §2.7）。§2.2 のゲイン変調
> $\gamma_k=\lVert a_k\rVert/\sigma_k$ は、idea_core §2.7.2 では**閾値へのマハラノビス計量の逆長さ**であり、
> 「修飾物質＝ゲイン」は形式的には「修飾物質が各閾値超平面のマハラノビス尺度を設定する」と読める（既存の
> 最も安全な対応の幾何的言い換え）。さらに一段強い仮説：活性は同じ二値化信号 $b=\mathbf 1[d+\eta>h]$ の
> **1 次モーメント（平均＝単調・閾値活性）**か**2 次モーメント（分散＝バンプ・交差活性）**かで質が変わる（§2.7.1）。
> もし修飾状態がこの**読みの次数**を偏らせるなら、§2.6 の「同一回路・複数出力（STG）」や §2.5 の「符号化↔想起の
> 経路切り替え」に、重み不変のまま**単調モード↔チューニング（バンプ）モード**という新しい計算的読みを与えられる
> （例：ACh 高＝鋭い特徴選択＝2 次バンプ／低＝滑らかな伝達＝1 次単調）。デモ候補として「場だけでモーメント次数を
> 動かし出力の質を切り替える」を §7 に検討する価値がある。
>
> **【2026-08-06 追記】「二択」ではなく連続ノブであることが確定した（`idea_core.md` §2.7.1a）。** 読み出しゲート
> $\lambda\in[0,1]$（点灯カウンタと反転カウンタの混合比）が 1 次↔2 次を滑らかに繋ぐ：
> $\bar z_\lambda=(1-\lambda)Q(s)+\lambda\,2Q(s)(1-Q(s))$。§2.5 の 2 本のカウンタは既に両方数えているので
> **追加回路ゼロ**——修飾物質の作用点として最も安価。$\lambda$ は同時に「読む時間相関の次数」（1 点＝積分器 ↔
> 2 点＝一致検出器）でもあり、素子が活性関数を持ち替えるのではなく**下流の検出器の読み方が動く**という、
> 生理学的に無理のない実体を持つ。**実測（`tmp/reservoir_lambda.py`）**：$\lambda\lesssim0.5$ は深さによらず
> 深層計算が健全で、劣化は $\lambda\gtrsim0.75$ の強バンプ域のみ。当初予想した $\lambda{=}1/3$ の鋭い臨界点
> （単調性の境界としては厳密に正しい）では性能は崩れず、**変調の安全域は広い**。切り立った崖の縁ではなく
> 広いプラトーを動かせるという意味で、修飾機構としてはむしろ好都合である。
>
> **【重要な訂正・2026-08-06】$\lambda$ の内点最適は成立しない**（`idea_core.md` §2.7.1a、
> `tmp/reservoir_lambda_gauge.py`）。一度は「局所表現課題で中間 $\lambda\approx0.6$ が両端も tanh も 5σ 級で
> 上回る」と記録したが、**実効ゲイン $\gamma$ を各 $\lambda$ で最適化してから比べると優位は消え、
> $\lambda{=}0$（単調）が一様に最良**になる（$\min_\gamma$ で 0.09 vs 0.14 vs 0.17）。前の結果は
> BatchNorm + 低ゲイン初期値という**条件の悪い動作点でだけ**成立していた。機構として述べた
> 「段＝勾配の手すり」も反証された（閾値の移動が最大なのは純バンプ $\lambda{=}1$ で 44%、$\lambda{=}0$ は 5.9%）。
>
> **残る正しい主張（弱いが本物）**：**$\lambda$ は実効ゲイン $\gamma$ の代用品**である。$\gamma$ を上げられない
> 動作点では 2 次モーメントを混ぜることが局所選択性を安価に買う（$\gamma{=}1$ で 0.51→0.21）が、
> $\gamma$ を上げられるならそちらが上位互換。修飾物語への含意は率直に言って**弱まった**——「モーメント次数を
> 変調しないと損をする」とは（この課題では）言えない。$\lambda$ 変調に意味があるとすれば
> **ゲインを上げられない制約下（飽和・量子化・生理的上限）での代替手段**という位置づけになる。
> なお $\lambda$ が連続ノブであること自体（§2.7.1a の補間・$1/3$ の単調性境界・「段の上のこぶ」の形の分解）は
> 解析的事実として有効で、この訂正の影響を受けない。]

### 2.6a 【一つの見方】1 次と 2 次の生物学的な位置づけ——交差活性の立つ瀬

確立した事実ではなく、$\lambda$ の両端（§2.7.1a）をどう位置づけるかの**解釈**として記す。
詳細版は `idea_core.md` §2.7.1c。

**1 次（閾値活性）＝発火率符号**：$z=\mathbb E[b]=Q(s)$ は「ノイズ下の膜電位が閾値を超える確率」＝
**発火率**そのもの。§2.2 のゲイン変調（背景シナプス入力が $f$-$I$ 曲線の傾きを決める）と直結し、
本稿で最も安全な生物対応である。**裏返せば「確率的発火率モデルの再記述」に留まりやすい。**

**2 次（交差活性）＝発火の不確定性**：$z=2\mathrm{Var}(b)=P(b_1\neq b_2)$ には単一ニューロンの直接的な
対応物が無い（分散は複数試行/素子にわたる統計）。加えて `idea_reservoir.md` §10.33 により、
実効ゲイン $\gamma$ を揃えると**性能でも 1 次に一様に劣る**ことが判明した。

**それでも 2 次が立つ筋（本稿にとって重要な順）**：

1. **$\sigma\to0$ で無条件に沈黙する。** 1 次は $d>h$ で決定論的に発火し続けるので on-brand には
   $h=h_0/\sigma$ の**2 変数協調**が要るが、2 次は**$\sigma$ 単独**で消える。したがって交差活性の
   生物学的な身分は「発火率の代替」ではなく **「ノイズ依存的な参加ゲート」**——
   **修飾物質がノイズ源（背景入力）を供給/停止するだけで、回路のどの部分が計算に参加するかが決まる**。
   これは §2.5（Hasselmo の符号化↔想起）・§2.7（容積伝達）・§5（結びつけ先は $\sigma$ ではなく $\rho$/$\nu$）
   という本稿の骨格に、**素子レベルの実体**を与える。L1（場だけで機能が変わる）の最も素直な実装。
2. **分散を読むのは単一ニューロンではなく回路**：$P(b_1\neq b_2)$ は 2 サンプルの不一致なので、
   **一致検出器**（MSO、樹状突起 NMDA スパイク、STDP の前後一致検出）が読む量として実装しうる。
3. **バンプ＝チューニング曲線**は生物の基本語彙（方位選択性・場所細胞・時間細胞）。ただし生物では
   通常**回路（興奮–抑制、側抑制）**が作るので、**機能的等価物の提案**に留めるべきで、
   「生物が単一素子でそうしている」とは主張しない。

**性能面の足場（2026-08-07 追記）**：`idea_reservoir.md` §10.34 で、$\gamma$ を揃えた比較でも 2 次が勝つ条件が
1 つ見つかった——**NARMA depth 1**（交差 0.34 対 閾値 0.39・tanh 0.37）。NARMA は積を含み、$2Q(1{-}Q)$ が
中心付近で二次形式ゆえ**積・二乗を直接供給する**ため。生物側でも**乗算的相互作用**（シナプス前抑制、
樹状突起の非線形統合、ゲイン場）は基本演算なので、「2 次モーメント＝積を作る素子」という読みは
上記の筋 2・筋 3 と両立する。ただし勝ち幅は小さく、$\gamma$ 許容窓の狭さという代償は残る。

**本稿の書き方への含意**：交差活性を**「一般に良い活性関数」としては売らない**（多くの条件で $\gamma$ を
揃えると負ける）。
**「ノイズが無ければ存在しない計算素子」**として位置づけ、性能面は 1 次モーメント側に委ねる分業が
誠実かつ強い。§2.7.1a の $\lambda$ 補間により**同一回路が追加コストゼロで両方を出せる**ので、
この分業は実装上も自然である。

### 2.7 容積伝達（「場である」ことの根拠）

Agnati・Fuxe らの **volume transmission**。放出が点対点でなく拡散性・勾配的であること、
つまり修飾は場だという主張の一次資料。「空間的に広がるノイズ場（バンプ状の動員パターン）↔
拡散性・容積伝達による広域かつ勾配的な放出」という対応を支える。

### 2.8 逆 U 字の実例（SR 主張の生物側の受け皿）

- DA と作業記憶: **Vijayraghavan et al. (2007)**、**Cools & D'Esposito (2011)**。
- 覚醒と検出成績: **McGinley, David & McCormick (2015)** が瞳孔径（NE 系覚醒の代理）に対する
  検出成績の逆 U 字を膜電位レベルで示した。**引用の役割は「逆 U 字の in vivo 実在」まで**:
  同実験では膜電位ゆらぎが**中間覚醒で最小**なので、「覚醒∝ノイズ」と読ませると SR と
  逆向きに刺さる（§1.1b ⑤）。SR の実証としてはザリガニ〜パドルフィッシュ系列（§3.2）を使う。
- 古典: **Yerkes & Dodson (1908)**。元の法則は単なる逆 U 字ではなく、
  **最適覚醒度が課題難度に依存して移動する**という 2 次元の主張である（§7.6）。

**本稿にとっての含意**: L3 の SR 曲線に「生物でも同じ形が測られている」という受け皿がつく。
これが無いと SR はモデル内部の性質にとどまる。

---

## 3. B. ノイズ場に結びつけると特に相性が良い題材

修飾物質をゲインでなく**ゆらぎ**に結びつけるとき、他の定式化では出てこない固有の主張は
「最適強度がある」「弱信号を通す」「探索を生む」の 3 つである。それぞれ生物側に実例がある。

### 3.1 歌鳥の LMAN（最良の例）

**Ölveczky, Andalman & Fee (2005)**、**Kao, Doupe & Brainard (2005)**、**Fee & Goldberg (2011)**。
大脳基底核系の LMAN が歌に**変動性を能動的に注入**し、しかもその注入量が社会的文脈
（メスの有無）で数十ミリ秒スケールで切り替わる。

つまり「**ノイズは除去すべき外乱ではなく、専用回路が生成し文脈で制御する機能的資源である**」という
NNN の第一原則（正典の 4 つの核の 1 つ）が、そのまま実験事実として存在している。

### 3.2 弱信号検出の SR

ザリガニ機械受容器 **Douglass et al. (1993)**、コオロギ尾葉 **Levin & Miller (1996)**、
ヒト触覚 **Collins et al. (1996)**、高齢者の姿勢制御 **Priplata et al. (2003)**、
総説は **McDonnell & Ward (2011)**。
**行動レベルの直接先例は Russell, Wilkens & Moss (1999)**: パドルフィッシュの
プランクトン**摂食行動そのもの**が環境電気ノイズの中間強度で最良になる。
本取組の行動デモ（採餌数 vs 濃度の逆 U 字）の位置づけに最も近い。

閾値下の信号がノイズ無しには伝わらないという点で NNN の障壁と機構が同一なので、
検出課題（$d'$ 対 $\sigma$ の逆 U 字）は**最も正直な SR デモ**になる（§7.4）。

### 3.3 ノイズの生理学的実体は背景シナプス入力であり、それ自体が修飾される

**Chance, Abbott & Reyes (2002)** は釣り合った背景入力（＝ノイズ）がゲインを乗算的に変えることを
実験で示した。**Destexhe, Rudolph & Paré (2003)** の高コンダクタンス状態、
**Ho & Destexhe (2000)** の応答性増強がこれを支える。
つまり「$\sigma$ を変える」は生理学的に実在する操作である。

### 3.4 修飾物質は実際に変動性・相関を変える

**Goard & Dan (2009)**、**Polack, Friedman & Golshani (2013)**、
**Minces, Pinto, Dan & Chiba (2017)**。注意については **Cohen & Maunsell (2009)**。
空間的に構造化されたノイズ場を「注意のスポットライト」として見せる案の根拠になる。

### 3.5 行動状態の二値切り替え（配線が完全に分かっている系）

**Flavell et al. (2013)**: C. elegans の roaming/dwelling がセロトニンと神経ペプチドで切り替わる。
現デモに近い「採餌モードの切り替え」を、コネクトームが既知の系で正当化できる（§7.5）。

---

## 4. C. 現デモの結びつけ方をどう扱うか

labeled-line 構成（食物 / 脅威 / シェルターに 1 つずつ場）を残すなら、根拠はモノアミンではなく
**視床下部のペプチド性状態系**に求めるべきである。

- **AgRP ニューロン**は採餌を駆動し、しかも食物を「見た」瞬間に活動が落ちる
  （**Betley et al. (2015)**、**Chen et al. (2015)**）。内的欲求が感覚で更新されるという
  現デモの閉ループ規則（`neuromod_weights`）とよく合う。
- **オレキシン**は覚醒・探索、**オキシトシン**は社会行動。
- 防御行動側は **Mobbs et al. (2007)** の threat imminence（脅威の切迫度に応じて前頭前野優位から
  PAG 優位へ切り替わる）が使える。これは現在の `threat_gain` / `threat_range` による
  **連続的な緊急度**の実装と実はよく一致している。

**処方**: $\alpha$ が近似 one-hot である限り、場はキーとして働く。少なくとも次のどれかを行う。

1. $\alpha$ を明確に混合的にして、場の補間が中間行動を生むことを定量化する（L4、§8(c)）。
2. 行動選択そのものではなく、**同一行動の遂行品質**に場を効かせる図を足す（§7.1）。
3. 対応づけ自体を roaming/dwelling のような**状態**の軸に置き換える（§7.5）。

---

## 5. D. 理論側の制約 — 結びつけ先は $\sigma$ ではなく $\rho$ / $\nu$

### 5.1 「修飾物質 $=\sigma$」は単独では意味を持たない

正典 §4 のゲージ構造から、$\sigma_k$ の絶対値はゲージ依存量である。
$h/\sigma$ を保ったスケーリングは交差率 $\nu$ を変えず、出力もビット単位で不変になる
（`idea_duality.md` §12.6 の実測: 出力不変のまま素朴な overlap が $0.8305\to0.8720$ に動いた）。
したがって生物学的に結びつけるべきは **$\rho$（動員ダイヤル、制御量）**か
**$\nu_k=\mathbb E[z_k]$（交差率、観測量）**であり、実質的には

> **閾値までの距離を、膜電位ゆらぎの標準偏差で測った量**

である。

### 5.2 生物側にも二つのつまみが実在する

これは生物側にも実体があるのが良いところで、修飾物質は実際に二つのつまみを別々に回す。

| NNN | 生理学的対応 | 文献 |
|---|---|---|
| $h_k$（交差しきい値の半幅） | 平均膜電位・興奮性・後過分極電流の変調 | McCormick (1992) |
| $\sigma_k$（注入ノイズ強度） | 背景シナプス入力の分散、高コンダクタンス状態 | Chance et al. (2002)、Destexhe et al. (2003) |
| $\rho_k=$ 両者の連動 | 「ゆらぎ駆動レジームにおける閾値までの実効距離」 | Shadlen & Newsome (1998) 以降 |

**「修飾物質は興奮性とゆらぎを同時に動かし、機能的に効くのはその比である」**という言い方は、
NNN のゲージ理論と in vivo の fluctuation-driven regime の議論の両方に同時に支えられる。
報告時はゲージ不変な比 $h/\sigma^*\approx0.17$–$0.25$ を併記すること（§6）。

### 5.3 参加度を $\sigma$ のしきい値判定で測ってはいけない

行動デモのノイズ場は `--theta`（**以下 θ_cut と読む**。正典 §1.1 の $\theta=\{W,b\}$ と衝突するため。
CLI 引数名は変更しない）で強度を切り捨て、$\sigma_k=0$ のユニットを作る。
しかしこれを「ユニットが未動員／detach された」と読んではいけない。理由は 2 つ。

1. **層のスコープ**: 実装は `net(obs, stds=[field, field])` と**同一の場を隠れ 2 層の両方に**適用する
   （`structure=[6,H,H,2]`、$h$ は両層共通の大域定数）。$\sigma_k=0$ が理想形どおり $z\equiv0$ を
   与えるのは**第 1 隠れ層だけ**であり、第 2 隠れ層では上流のサンプルゆらぎが $\pm h$ をまたぐため
   $\sigma_k=0$ でも交差が残る（$\sigma=0$ リーク、実測 $\nu=0.11$–$0.17$ のプラトー。正典 §3.4）。
   厳密に沈黙させるには **kill の三点操作**（$\sigma_k\leftarrow0$、$h_k\leftarrow H_{\mathrm{DEAD}}=10^6$、
   $W^{(l+1)}[:,k]\leftarrow0$）が要る（正典 §3.5）。
2. **測り方**: 参加／不参加はゲージ不変な $\nu_k$ で測る（正典 §3.3, §4.3, §4.8）。

したがって θ_cut は「場の空間的な支持を切り出すためのバンプ裾の切り捨て」であって、
ユニットの退役操作ではない。`--model analytic` では期待応答が $\sigma\to0$ で（$d\neq0$ なら）
層によらず 0 に収束するので理想形と一致するが、本命図の `--model sample` は上の限定を受ける。

---

## 6. 支持証拠 — SR は sample 水準にしか存在しない（実測済み）

> **【2026-08-08 改訂】** 本節の「学習水準スイープの内点最適」は旧 vector 設定の結果で
> あり、**標準ベンチマーク＋完全学習では再現しない**（学習水準は平坦＝ゲージ補償、
> §7.13 E2）。SR 主張の存立形は §7.13 の 3 点（普遍相対用量曲線・$\sigma{=}0$ の
> 不可能性と適応床・沈黙崩壊の署名）に移行した。本節は歴史的記録として残す。

L3（資源性）を支える実験はすでに手元にある。

`--sweep train`（各ノイズ水準で新規ネットを学習し、その水準で採点）において:

- **`--model analytic`（平均場）は低 $\sigma$ 端で最適**。低 $\sigma$ でも重みの再スケールで学習でき、
  真の SR 障壁を持たない。
- **`--model sample`（実ノイズ注入＋閾値 $h>0$）は内点最適**（$\sigma^*\approx0.8$–$1.2$）で、
  低 $\sigma$ 側が実際に崩壊する（閾値下は伝達不能）。

すなわち **SR は平均場近似では消え、サンプルレベルの機構でのみ現れる**。
厳密な SR 図は `--sweep train --model sample`、`analytic` はその対照である。
テスト時スイープ（1 ネットで学習水準を固定し、テスト時のノイズだけを掃引）は全モデルで
逆 U 字を示すが、「学習水準の近傍で誤差が最小になるのは構造上当然」という交絡があるため、
補助図に留める。

**体制の明示が必須**（正典 §4.7）。`--sweep train` は `--crossing-h`（既定 0.2）を
**大域ハイパーパラメータとして固定したまま** $\sigma$ だけを掃引し、各点で $\theta=\{W,b\}$ を
再学習する ＝ **体制 (b)**（$h$ 固定 + $\theta$ 学習、ゲージ不在、$\sigma$ は本物の自由度）である。
もし $h\propto\sigma$ と連動させれば掃引は純粋な**ゲージ軌道**になり（$h/\sigma$ 一定 → $\nu$ 不変、
正典 §4.6）、曲線は平坦化して**内点最適そのものが定義上消える**。
報告時はゲージ不変な比 $h/\sigma^*\approx0.17$–$0.25$ を併記し、$\sigma^*$ の絶対値だけを
主張の担い手にしないこと。

**実測例（seed 7、`--sweep train --model sample`、$h=0.2$ 固定）**:

| 指標 | 端点 | 最適点 |
|---|---|---|
| 刺激依存信号 | 0.063 / 0.790 | **0.803**（$\sigma_{\text{train}}=1.19$、$h/\sigma^*=0.17$） |
| タスク誤差 | 0.414（低 $\sigma$ 崩壊）/ 0.030 | **0.014** |
| 場の分離度 | — | 1.430 |

※ 単一 seed の例。投稿には複数 seed の mean±std が要る（付録 A の R4）。

**別系統からの裏付け**: `idea_reservoir.md` §13.1（2026-07-30）で、**同一重み**の診断
（analytic で学習・凍結 → 同一重みのまま大域ノイズ強度 $\kappa_\sigma$ を掃引して両応答を採点）が
実装され、**sample は逆 U 字**（$\kappa_\sigma^*\approx0.20$–$0.23$、6 seed × 2 タスクすべて）、
**analytic は平坦なプラトー**が確認された。本稿の train スイープ（各水準で再学習）とは
別系統の手法であり、互いに補強する。本稿の文脈での同一重み診断は未実施。

---

## 7. デモ案

### 7.0 選定基準

良いデモの条件は 3 つある。**(i) L1–L4 のどれかの空白を埋めること**、
**(ii) 生物側に対応する実験事実があること**、**(iii) 既存資産で作れること**。
以下は RL を使わない案を優先度順に並べたものである（RL 系を後段に置く理由は §7.8）。

### 7.1 【推奨 1】行動レベルの SR ＝ 濃度スライダー・デモ

同一重み・同一環境で修飾物質「濃度」だけをスライドさせ、**両端で行動能力が壊れ、中間で最も
上手くやる**ところを見せる。曲線ではなく**動画として見せられる**のがこの案の核心である。

- 低濃度: 交差が起きず出力が沈黙し、エージェントが動けない（閾値下の崩壊）
- 適正: 採餌・回避・帰巣がきれいに回る
- 高濃度: 交差が飽和して出力が定数化し、行動が無方向・不規則になる

**指標**は既存の閉ループから素直に取れる。`tmp/neuromod_behavior_modes.py` はすでに
`food_strengths` / `hunger` / シェルター滞在 / 脅威接近を持っているので、
「1000 フレームあたりの摂食数」「シェルター到達までの時間」「脅威との最接近距離」を
$\sigma$ の関数にするだけでよい。

**なぜ良いか。** (1) L3 の縦軸が「ベクトル場回帰の MSE」という機械学習の指標から
**行動の指標**に変わり、神経科学の読者には Yerkes–Dodson (1908) の逆 U 字そのものに見える。
(2) §1.2 の自己限定（行動は設計であって創発ではない）と衝突しない。設計された行動の
**遂行品質**にノイズの最適値があると言っているだけだからである。
(3) 特集の主眼（修飾物質濃度と行動能力の関係）に最も近い。

**実装（2026-08-02）**: `tmp/neuromod_behavior_sr.py`。共通部に閉ループの 1 フレーム
`world.advance_frame` とヘッドレス測定 `world.rollout` を置き、**動画と測定が同じ力学を使う**ことを
保証した（`viz.animate` も同じ関数を通る）。動画は `viz.concentration_slider` の 4 パネル
（世界＋軌跡、現在濃度のノイズ場、速度トレース、行動曲線上を動くカーソル）。

**急性投薬版の実測（sample、2500 エポック、9 濃度 × 3 エピソード × 1500 フレーム）**:

| 濃度 | 0.15 | 0.24 | 0.38 | 0.60 | 0.95 | 1.50 | 2.39 | 6.00 |
|---|---|---|---|---|---|---|---|---|
| foods/1k | 0.22 | 1.11 | 4.67 | 8.67 | **9.56** | 4.67 | 0.00 | 0.00 |
| 移動距離 | 8.6 | 11.4 | 18.5 | 31.1 | 32.9 | 22.5 | 14.8 | 2.0 |

ピークは濃度 0.95（$h/\sigma=0.211$、§6 の帯 0.17–0.25 に一致）。動画では低濃度で速度がゼロに
張り付いて採餌が止まり、最適濃度で脅威を迂回する採餌ループが回り、高濃度で無方向になる。

> **この曲線は SR 主張を支持しない**（§6 の交絡）。ある濃度で育った動物を別の濃度で走らせれば、
> 平均場でも「実家から遠い」ほど成績が落ちるので、test スイープは構造上逆 U 字を出す。
> これは動画が見せている**急性投薬**の用量反応であって、主張には `--sweep train`
> （各濃度で新規に学習）と analytic 対照が要る。スクリプトは test スイープのとき明示的に警告する。

**厳密版（train スイープ、各濃度で新規学習）の実測 — 示唆的だが未確立**。
6 濃度、2000〜2500 エポック、各濃度 1 ネット × 3 エピソード:

| 濃度 | 0.150 | 0.314 | 0.656 | 1.372 | 2.869 | 6.000 | 端/ピーク比 |
|---|---|---|---|---|---|---|---|
| sample | 3.33±1.44 | 7.11±1.13 | 6.89±1.37 | **7.56**±1.13 | 6.44±1.91 | 4.22±0.63 | 0.44 / 0.56 |
| analytic | 4.22±0.63 | 6.89±0.31 | **7.56**±0.31 | 6.89±0.83 | 6.89±1.13 | 6.22±1.37 | 0.56 / 0.82 |

**方向は正しいが、主張はできない。** sample のほうが両端で落ちる（特に高端 4.22 対 6.22）ものの、
各濃度 1 ネットで、示している幅は**同一個体のエピソード間ばらつき**にすぎず、個体間変動を含まない。
低端の差（3.33±1.44 対 4.22±0.63）は有意でない。**`--seeds 3` で回し直すまでは結果と呼べない。**

**なぜ行動指標では弱いのか（2 つの仮説、どちらも重要）**:

1. **交差活性は低 $\sigma$ でも計算できる。** $\phi$ は前活性が帯 $\pm h$ の**内側**にあるとき発火するので、
   $\sigma$ が小さいネットワークは「狭帯域検出器」として学習し直せる。各動物が自分の濃度で育つ
   train スイープでは、この適応が働いて崩壊しない。§6 の低 $\sigma$ 崩壊は task error で測ったもので、
   行動では回避されうる。
2. **行動指標が粗く、頭打ちになっている。** `foods_per_1k` は 7 前後で飽和して見える（移動時間律速）。
   方策の劣化が採餌数に出にくい。到達時間や経路効率のような鋭い指標のほうが適する可能性がある。
   ただし**望む答えが出る指標を後から選ぶのは禁じ手**なので、指標を変えるならその理由を先に固定すること。

したがって現時点では、**§7.1 は動画としては完成、主張としては保留**である。

**この実験で判明した落とし穴（いずれも設定の問題で、ノイズの効果ではない）**:

1. **平均場では原理的に出ない。** `SimpleNNNSample` への載せ替えが前提（§6）。
2. **学習不足が「壊れたデモ」に見える。** analytic は task MSE をほぼ 0 にするのに 2500 エポック
   必要で、500 エポックの sample では採餌ゼロ・脅威に接近と、ノイズと無関係な理由で破綻する。
   `--epochs` の既定を 2500 にし、`--net-path` / `--load-net` で学習を再利用できるようにした。
3. **`--speed-gain` は cruise 用の値では小さすぎる。** learned モードの歩幅は $|v|$ に比例し、
   閉ループでは対象物の近くにいる時間が長く学習した減速則が効くため $|v|$ が小さい。既定 0.9 では
   移動距離が cruise の 4 割しかなく採餌できない。2.7–3.6 が適正。
   なお「動物の通常速度で正規化する」対策は、学習グリッド上の $|v|$ 中央値が 0.99 とほぼ飽和値で
   閉ループの実効値と食い違うため**効果がなく、採用しなかった**。
4. **安全指標は移動距離で正規化すること。** `close_frac`（脅威 0.2 以内のフレーム比）は最適点で
   0.027、低濃度で 0.036 だが、距離あたりでは 0.00087 対 0.0042 と**動いている方が 5 倍安全**である。
   止まっている動物は接近機会自体が少ないだけで、生の比率は交絡する。

### 7.2 【推奨 2】ベースライン依存の効果反転（同じ「薬」が逆に効く）

**Cools & D'Esposito (2011)** の中核的発見は、単なる逆 U 字ではなく、**同一のドーパミン作動薬が、
ベースラインの低い個体では成績を上げ、高い個体では下げる**ことである。逆 U 字の左右どちらに
乗っているかで効果の符号が反転する。ヒトの神経修飾薬理で最も頑健で有名な現象のひとつ。

**実装**: `tmp/neuromod_baseline_shift.py`（2026-08-02 作成）。設計の要点は 2 つ。

- **ベースラインは形質**。各ネットを**自分の $\sigma_\text{base}$ で学習**する（学習水準スイープ）。
  こうして初めて「そのベースラインで出せる最良の性能」どうしの比較になる。
- **薬は急性の摂動**。重みを凍結し、$\sigma\to g\cdot\sigma$ とノイズ場だけを掃く。
  **$h$ は固定する**こと。$\sigma$ と $h$ を一緒にスケールするのは純粋なゲージ変換で、
  何も変わらない（正典 §4.6）。薬は $\sigma$ 単独を動かさなければ意味を持たない。

**対照がそのまま予測になる**: 符号反転には内点最適が要るので、`--model sample` では出て
`--model analytic`（平均場、閾値下の障壁なし）では出ないはずである。両方を走らせるのが誠実な形。

図は 2 段で、上段が学習水準の逆 U 字に各ベースラインからの投薬を矢印で重ねたもの（矢の根は
曲線上、先は曲線外＝凍結ネットを別の $\sigma$ で評価した点）、下段が改善量の符号反転である。

**なぜ良いか。** L3 の証拠として素の逆 U 字より**強い**。鍵仮説では「同じ介入が状況次第で
助けにも害にもなる」ことを説明できないからである（§1.3 の批判 (b) への決定打）。

**実行結果（2026-08-02、成立）**。$h=0.2$ 固定、7 ベースライン $\in[0.3,2.2]$、
300 エポック、grid 13、$T=24$。まず単一 seed・$g=1.4$ の内訳:

| $\sigma_\text{base}$ | 0.30 | 0.62 | 0.93 | 1.25 | 1.57 | 1.88 | 2.20 |
|---|---|---|---|---|---|---|---|
| sample: $\Delta$err | **−0.059** | **−0.031** | **−0.006** | +0.007 | +0.014 | +0.016 | +0.020 |
| analytic: $\Delta$err | +0.016 | +0.036 | +0.055 | +0.061 | +0.063 | +0.064 | +0.061 |

- **sample は符号が反転する**。同じ用量が $\sigma_\text{base}\le0.93$ では成績を上げ、
  $\ge1.25$ では下げる。反転点は $\sigma\in(0.93,1.25)$、すなわち
  $h/\sigma\in(0.16,0.22)$ で、§6 が別系統（学習水準スイープ）で得た
  $h/\sigma^*\approx0.17$–$0.25$ の帯にそのまま入る。**独立な 2 つの測り方が同じ最適点を指した**。
- **analytic は反転しない**。しかも「効果なし」ではなく**全ベースラインで単調に悪化**する
  （task error はベースラインとともに 0.018→0.210 と単調増加、すなわち低 $\sigma$ 端最適）。
  これは §6 の「平均場は閾値下の障壁を持たない」の直接の帰結であり、**予測どおりの帰無対照**である。
  帰無が「何も起きない」ではなく「符号が一方向に固定される」形で出るのが、この対照の強いところ。

**多 seed・多用量版（3 seed、用量 4 点、同一の学習済みネットに後付けで投薬）**。
用量は凍結ネットへの急性摂動なので**再学習が要らず、用量軸はほぼ無料**である。

| 用量 $g$ | 1.2 | 1.4 | 1.8 | 2.5 |
|---|---|---|---|---|
| 反転点 $\sigma_\text{base}$ | 1.381 | 1.196 | 0.844 | 0.481 |
| 反転点での $h/\sigma$ | 0.145 | 0.167 | 0.237 | 0.416 |

- **符号反転は 4 つの用量すべてで、3 seed を通して成立する**（seed 帯は図で確認できる程度に狭い）。
- **反転点は用量とともに単調に低ベースライン側へ移動する**。これは逆 U 字が要求する第 2 の署名である。
  用量が大きいほど、より低いベースラインから出発しても最適点を通り越してしまうからである。
  鍵仮説はこの用量依存性も説明できない。
- $g=1.4$ の反転点 $h/\sigma=0.167$ は §6 の $h/\sigma^*\approx0.17$–$0.25$ の下端に一致する。

**残**: 課題難度を振ったときの反転点の移動（§7.6 の Yerkes–Dodson 2 次元版）は未着手。

### 7.3 【推奨 3】損傷・重なり実験（L2 の空白を埋める）

派手さはないが、**L1–L4 で唯一まだ埋まっていない主張**であり、必要な道具は全部揃っている。

**手順と定義**（素朴な定義を採るとどちらも成立しないので、ここで固定する）:

- **動員ユニット集合**は $S_i=\{k\mid \nu_k>0\}$（交差率の支持）と定義する。
  $\{k\mid\sigma_k>\text{閾値}\}$ で定義してはいけない。$\sigma_k$ の絶対値はゲージ依存量なので、
  集合そのものがゲージ変換で動き、Jaccard 係数が関数の量でなくなる（§5.1 の実測）。
  $\nu_k$ は 0 次同次なのでゲージ不変。$\cos(\mathcal P_A,\mathcal P_B)$ 型の場の重なりを
  指標にしないこと。
- **損傷（lesion）**は **kill の三点操作**（$\sigma_k\leftarrow0$、$h_k\leftarrow H_{\mathrm{DEAD}}=10^6$、
  $W^{(l+1)}[:,k]\leftarrow\mathbf 0$）で実装する。$\sigma_k\leftarrow0$ だけでは第 2 隠れ層のユニットは
  沈黙せず（$\sigma=0$ リーク、§5.3）、「損傷したのに壊れない」という結果になって実験自体が成立しない。
- **対照**: 損傷の効果は行動ごとの task error の増分で測り、$S_{\text{shared}}=S_A\cap S_B$ の損傷と
  $S_A\setminus S_B$ の損傷を対照する。区画分割仮説なら前者は空、または片方の行動しか劣化しない。

**生物側の受け皿**: Marder の STG（同一回路・複数出力）が直接の先行例で、さらに
Edelman & Gally (2001) の degeneracy、Marder & Taylor (2011) の「同じ振る舞いを複数の
パラメータ集合が実現する」議論に接続できる。損傷 + 重なりという実験の形自体が神経科学の
標準的な考え方なので、査読者に伝わりやすい。

**実行結果（2026-08-02、成立）**。`tmp/neuromod_lesion.py`。analytic・**隠れ 1 層**
（$\sigma$-only 動員が厳密に効く条件）、1200 エポック、grid 17、3 seed、リング半径 0.28。

> **群のサイズを揃えること**。shared は private より小さいので、生の損傷量を比べると
> 「どこを切ったか」と「どれだけ切ったか」が交絡する。全群を $|S_A\cap S_B|$ 個に揃え、
> private とランダムは複数回の抽選で平均する。
>
> **私有ユニット損傷の特異性は証拠ではない**。$\sigma$-only 動員では、ある行動の支持の外にある
> ユニットはその行動を評価する時点で既に沈黙しているので、除去しても厳密に 0 である。
> private 群は「k ユニット分の価値」を測る**物差し**であって、結果ではない。
> 証拠は「shared 損傷が**両方**を壊すか、その量が物差しに対して有意か」だけである。

$\alpha_\text{mix}=0.6$（3 seed 平均、task error の増分）:

| ペア | shared | private の物差し | random 対照 | 判定 |
|---|---|---|---|---|
| food\|threat | +0.115 / +0.084 | 0.079 / 0.078 | +0.068 / +0.040 | 多重化 |
| food\|shelter | +0.174 / +0.169 | 0.107 / 0.110 | +0.048 / +0.064 | 多重化 |
| threat\|shelter | +0.095 / +0.106 | 0.057 / 0.066 | +0.034 / +0.039 | 多重化 |

**共有ユニットの損傷は両行動を同時に劣化させ、しかもその量は各行動の私有ユニットの物差しを
上回り、同サイズのランダム損傷も上回る**。区画分割仮説ではこの署名は出ない（分割なら共有ユニットは
高々一方にしか属さない）。§1.3 の批判 (c) への直接の回答である。

**さらに重要な副産物: §4 の処方が L2 の成否を決めている。** $\alpha_\text{mix}$ を振ると
food|threat の判定が単調に変わる（shared の損傷量 vs ランダム対照、3 seed 平均）:

| $\alpha_\text{mix}$ | shared | random | 判定 |
|---|---|---|---|
| 既定（近似 one-hot） | +0.050 / +0.043 | +0.061 / +0.070 | **曖昧**（shared がランダムを下回る） |
| 0.3 | +0.060 / +0.058 | +0.052 / +0.075 | 多重化（弱） |
| 0.6 | +0.115 / +0.084 | +0.068 / +0.040 | **多重化** |

近似 one-hot では 3 つのタスクが**別々の入力次元**を読むので、場が重なっていてもネットワークは
共有する必要がない。「重なり」は使えるユニットの重なりであって、使われ方の重なりではない。
$\alpha$ を混合すると同じ入力次元を奪い合うようになり、初めて多重化が要請される。
**§4 で指摘した近似 one-hot の弱点が、L2 の実験結果として定量的に確認された**ことになる。
したがって L2 を主張する図は $\alpha_\text{mix}>0$ の条件で作ること。

### 7.4 弱信号検出（$d'$ 対 $\sigma$）

SR 文献の canonical パラダイムそのもの（§3.2）。閾値下刺激の検出感度を $\sigma$ の関数にする。
対応が比喩でなく**同一のパラダイム**なのが強みで、縦軸が $d'$ という心理物理の標準単位になる。
コストは低いが、行動デモ（§7.1）ほどの視覚的訴求力はない。掃引の骨格は
`tmp/neuromod_sr_curve.py` を流用できる。

### 7.5 roaming / dwelling への結び替え

**Flavell et al. (2013)** の C. elegans は、セロトニンと神経ペプチドで採餌の 2 状態
（速く直進的に探索する roaming と、遅く局所的に留まる dwelling）が切り替わる。
現デモの labeled-line を、この**状態**の軸に置き換えると、§4 で指摘した生物学的裏付けの薄さが
一気に解消される。環境もアニメーションもそのまま使え、しかも roaming↔dwelling は生物でも
連続的に遷移するので、場の補間による中間行動 ＝ **L4 の定量化**にそのまま乗る。
RL を使わずに探索↔利用の話ができるのも利点。

### 7.6 Yerkes–Dodson の難度依存性（novelty は最も高い）

元の法則は「難しい課題ほど最適覚醒度が低い」という**2 次元の予測**である。既存の掃引に
課題難度の軸を足すだけで、$\sigma^*$ が難度とともに移動するかを見られる。

**ただし「難度」の定義を先に決める必要がある。** 弱信号性を上げるとノイズが要る方向、
要求精度を上げるとノイズが害になる方向で、予測の向きが逆になる。裏を返すと、軸の定義を
明示することで NNN が**反証可能な予測**を出せる、という書き方ができる。

### 7.7 やらないほうがよいもの

**ACh の符号化 vs 固定化**（§2.5、Hasselmo）は生物学的には申し分ないが、$\rho$ による資源制御
（`idea_consolidation.md`）と主張が重なる。ここでデモ化すると 2 本の研究線の主張が混ざるので、
本稿では Discussion で一段落触れて `idea_consolidation.md` を指すに留める。

### 7.8 RL 系を後段に置く理由

§2.4 の探索温度デモ（`idea_rl.md` §25 の内部化温度をそのまま神経修飾の枠で提示し直す）は
生物学的根拠も既存資産も厚いが、**RL の機構そのものを説明する負担**が加わる。
§7.1–7.3 はいずれも教師付き学習のままで成立し、しかも L1–L4 の空白を直接埋める。
先に非 RL の 3 本で骨格を固め、探索温度は「同じ原理が RL でも成り立つ」という拡張として
後段に置くのが安全である。

### 7.9 まとめと着手順

| # | 案 | 埋まる段 | 生物側の根拠 | 再利用できる資産 | コスト |
|---|---|---|---|---|---|
| 1 | ベースライン依存の効果反転（§7.2） | L3 を決定的にする | Cools & D'Esposito (2011) | `tmp/neuromod_baseline_shift.py` | **実施済み（符号反転を確認）** |
| 2 | 損傷・重なり（§7.3） | **L2** | Marder、Edelman & Gally | `tmp/neuromod_lesion.py` | **実施済み（多重化を確認）** |
| 3 | 行動レベル SR（§7.1） | L3 を行動で見せる | Yerkes–Dodson、McGinley | `tmp/neuromod_behavior_sr.py` | **動画は完成、主張は保留** |
| 4 | 弱信号検出 $d'$（§7.4） | L3 の別証拠 | SR 文献直系 | `neuromod_sr_curve.py` の骨格 | 小 |
| 5 | roaming/dwelling（§7.5） | L4 ＋ §4 の解消 | Flavell et al. (2013) | 行動デモの環境・アニメ | 中 |
| 6 | Yerkes–Dodson 難度依存（§7.6） | L3 の新規予測 | Yerkes & Dodson (1908) | 既存掃引 + 難度軸 | 小（設計に注意） |
| 7 | 探索温度（§2.4、§7.8） | L3 の RL 版 | Aston-Jones & Cohen、LMAN | `tmp/rl_ppo_itemp_swingup.py` | 中（説明負担大） |

**着手順の推奨**: 1 → 2 → 3。**1（§7.2）と 2（§7.3）は完了**、3（§7.1）は動画のみ完成。
L1–L4 のうち残る空白は L4（連続制御の定量化、§8(c)）だけになった。

### 7.9.1 次の一手の判断（2026-08-03）

**判断: §7.1 の厳密版に追加投資せず、動画は「機構の直感的な提示」として使い、
資源は L4 の定量化に回す。**

根拠は 3 点である。

1. **L3 はすでに独立な 2 系統で成立している。** SR 曲線の内点最適（§6）と、ベースライン依存の
   符号反転（§7.2、3 seed、用量依存性つき）。§7.1 は L3 の**3 番目**の証拠であって、
   無くても主張は立つ。
2. **§7.1 の厳密版は費用が重く、成功も保証されない。** `--seeds 3` で 6 濃度 × 2000 エポックは
   **約 10 時間**。しかも現状の 1 seed 結果は sample と analytic の差が小さく（端/ピーク比
   0.44,0.56 対 0.56,0.82）、多 seed で誤差が縮んでも差が残るとは限らない。§7.1 に記した
   2 つの仮説（交差活性は帯の内側で発火するので低 $\sigma$ でも学習し直せる／`foods_per_1k` が
   移動時間律速で頭打ち）が正しければ、**行動指標では原理的に SR が見えにくい**。
3. **L4 は唯一の空白で、着手コストが低い。** `cycle` モード（場の補間）と `--alpha-mix` が
   すでにあり、`world.rollout` で行動指標も取れる。補間した場が生む行動を対応する補間目標に
   対して評価すれば、未学習の中間行動が滑らかに現れることを曲線にできる（§8(c)）。

**この判断が変わる条件**: 査読で「行動レベルの証拠が要る」と言われた場合、または L4 が早く
片付いて時間が余った場合。その際は §7.1 に戻り、まず**指標を替える**こと（到達時間・経路効率）を
検討する。ただし §7.1 に書いたとおり、望む答えが出る指標を後から選ぶのは禁じ手なので、
指標変更の理由を先に固定してから回すこと。

**動画の扱い**: `behavior_sr_slider.gif` は Results ではなく、Introduction か補足動画に置く。
キャプションには「急性投薬の用量反応であり、SR の主張は §6 と §7.2 が支える」と明記する。

> **コストの実測**（2026-08-02）: `--model sample` の学習は **約 1 秒/エポック**で、
> グリッド数や $T$ にほとんど依存しない（テンソルサイズではなくステップあたりの
> オーバヘッドが律速）。つまり **エポック × ベースライン × seed がそのまま秒数**になる。
> 掃引系のスクリプトを設計するときはこれを前提に規模を決め、重い実行は背景に回すこと。

---

### 7.10 実装済みの前提整備（2026-08-02）

§7 のデモに着手する前に、標準問題の側で 3 つの設定を直した。いずれも「主張を潰していた設定」である。

1. **場の中心をリング配置にした**（`fields.ring_centers`、`--field-radius`）。旧設定は直角三角形配置で、
   food–threat だけが対角線上に離れ、共有 2 ユニット（Jaccard 0.043）に対し他ペアは 8（0.200）だった。
   L2 の損傷実験で最も試したいペアが**ほぼ分割**されていたので、そのまま実験すれば反証したい区画分割
   仮説のほうを支持してしまう。リング配置後は 0.227 / 0.227 / 0.182 とほぼ対称になり、
   **重なり量が `--field-radius` という制御変数になった**（L2 の掃引軸として使える）。
   旧配置は `--corner-centers` で再現できる（0.043 / 0.200 / 0.200 を確認済み）。
2. **参加度を交差率 $\nu_k$ で測るようにした**（`fields.overlap_report`）。旧実装は
   `(field > 0).sum()` という $\sigma$ しきい値で「動員ユニット数」を数えており、§5.3 と §7.3 で
   自ら禁じた定義だった。なお analytic・第 1 隠れ層では両定義は一致する（$\sigma_k=0 \Rightarrow \nu_k=0$）。
   $\nu$ 定義が効くのは sample 水準・深い層・ゲージ変換下である。
3. **閉ループが出力の大きさを使うようにした**（`world.step_agent`、`--speed-mode learned`）。
   旧実装は予測ベクトルを正規化して向きだけを使い、学習した $\tanh$ 速度則を捨てていた。
   これでは低 $\sigma$ の沈黙が「凍りつく」ではなく単なる jitter にしか見えず、**逆 U 字の左腕が
   隠れる**。アニメに速度パネルを追加し、崩壊が乗るチャンネルを可視化した。旧挙動は
   `--speed-mode cruise`。

あわせて、**kill 三点操作を `fields.kill_units` として実装**した（$\sigma_k\leftarrow0$、
$h_k\leftarrow H_\mathrm{DEAD}$、$W^{(l+1)}[:,k]\leftarrow0$）。共有ユニットに適用すると
$\nu=0$・下流列 0 になることを検証済みで、§7.3 の損傷実験はこれを呼ぶだけで書ける。
`--hidden-layers 1` と `--model sample` も選べるようにしたので、$\sigma=0$ リークを避ける条件で
実験できる。

`--alpha-mix` は §4 の処方 1（$\alpha$ を混合的にする）を掃引可能にしたもので、既定は旧来の
近似 one-hot のままである。

### 7.11 L4 の定量化 — 場の補間は連続な制御軸だが、中間行動は劣化する（2026-08-04）

`tmp/neuromod_interpolation.py`。§8(c) に対する実験で、L1–L4 で唯一残っていた空白。

**設計**。学習は従来どおり 3 つの**純粋場だけ**（`protocol.train` は混合場を一度も見ない）。
推論時に、ペア $(c_a,c_b)$ と $\lambda\in[0,1]$ について場 $F(\lambda)=(1-\lambda)F_a+\lambda F_b$ を与え、
駆動重みを同じ $\lambda$ で混ぜた $t(\lambda)$ に対して評価する。$t(\lambda)$ は目標写像が tanh で
頭打ちするため $t(0),t(1)$ の線形補間**ではない**ので、これを当てるのは非自明な予測である。
$\lambda$ 軸は推論だけなので**追加学習はゼロ**。

対抗仮説は「場は離散セレクタであり、中間の場はどちらかの学習済み行動を選ぶ（＝出力が 1 回だけ跳ぶ）」。

**事前に固定した判定式**（後から緩めないこと）:

| | 指標 | 合格条件 |
|---|---|---|
| M1 | $\mathrm{MSE}(y(\lambda),t(\lambda))$ | 全 $\lambda$ で端点（学習済み）誤差の 1.5 倍以下 |
| M2 | 含意混合率 $\hat\lambda(\lambda)=\arg\min_\mu \mathrm{MSE}(y(\lambda),t(\mu))$ | Spearman $\ge 0.9$ |
| M3 | 最大 1 ステップ跳躍 / 総経路長 | 一様ステップの 3 倍以下（$n_\lambda=21$ で 0.15。スイッチなら $\approx 1.0$） |

M2 を主指標にしたのは、対応が**線形**であることまでは要求しないためである。単調だが歪んだ
つまみでも L4 は成立し、$\hat\lambda$ はそれを測る。

**条件**: `--model sample`、単層、$\alpha_\text{mix}=0.6$、3 seed、2500 エポック（学習損失 0.006）、
評価 $T=2048$。学習ネットは `tmp/out/interp_net_sample_mix06.pt`。
**analytic は使わない**。平均場では場が $\sigma$ を通じて滑らかに入るので連続性がほぼ構成上自明で、
帰無対照にも主図にもならない（配線確認に 1 回使って結果は破棄した）。

**結果**（3 ペア共通、`tmp/out/interp_sample_mix06.png`）:

| | food\|threat | food\|shelter | threat\|shelter | 判定 |
|---|---|---|---|---|
| 端点誤差 | 0.0021 | 0.0019 | 0.0021 | — |
| M1 内点誤差（最悪 / 平均） | 0.0234 / 0.0146 | 0.0210 / 0.0136 | 0.0238 / 0.0153 | **FAIL** |
| M2 Spearman（相異なる $\hat\lambda$ の数） | 1.000 (21) | 1.000 (21) | 1.000 (21) | **PASS** |
| M3 最大跳躍 | 0.077 | 0.082 | 0.083 | **PASS** |
| 補間目標が近い方の学習済み目標に勝つ内点の割合 | 100% | 100% | 100% | — |

**判定は割れた。事前登録の基準では L4 は成立していない。**

- **順序と連続性については決定的**。$\hat\lambda$ は 21 点すべてが相異なり、Spearman は 3 ペアとも
  厳密に 1.000、曲線はほぼ対角（food\|threat のみ上に弓なり＝単調だが歪んだつまみ）。跳躍は
  一様ステップの 1.6 倍程度で、スイッチが要求する $\approx 1.0$ とは 1 桁違う。さらに補間目標が
  近い方の学習済み目標を**内点の 100%** で上回る。**離散スイッチ仮説は明確に否定された。**
- **精度については不成立**。内点誤差は端点の約 7 倍。誤差は $\lambda=0.5$ を頂点とする滑らかな弧を描く。

**事後診断 3 つ**（判定には入れない。M1 が落ちた後に追加したものと明示する）:

1. **含意混合率での誤差 ≈ 補間誤差**（0.0134–0.0150 対 0.0136–0.0153）。つまり出力は
   「軸上の位置がずれている」のではなく、**どの混合率で見ても形が違う**。$\hat\lambda$ が
   ほぼ対角なのと整合する。
2. **素朴な混合器の方が正確**。学習済み出力 2 つを線形に混ぜるだけの参照は 0.0036–0.0080 で、
   実際のネットワーク（0.0136–0.0153）より 2–4 倍良い。**これが最も不利な数字である。**
   ネットワークは混合場の下で、自分自身の 2 つの答えを混ぜる以上のことをしていない、
   どころか下回っている。
3. **動作点仮説は反証された**。$\sigma$ を線形に混ぜると単独支持のユニットの $\sigma$ が下がるので、
   学習した $h/\sigma$ 動作点から外れることが内点誤差の原因ではないか、と考えた。しかし
   平均交差率 $\nu$ は内点でも端点の 86–98% で、ほとんど落ちていない。さらに参加度を保つ
   代替則（ノイズ**パワー**の補間、$\sigma(\lambda)^2=(1-\lambda)\sigma_a^2+\lambda\sigma_b^2$）を
   走らせると $\nu$ は 100–112% に保たれる一方、誤差は 0.046–0.054 と**3 倍悪化**し M3 も落ちる
   （`tmp/out/interp_sample_mix06_var.png`）。線形則が正しく、動作点の話ではない。

**言えること / 言えないこと**。言えるのは「**ノイズ場は離散セレクタではなく、単調で連続な制御軸である**」
（L4 の質的部分）。言えないのは「**補間した場が、対応する中間行動を学習済みと同等の精度で生む**」
（L4 の量的部分）。原稿では前者だけを主張し、後者は主張しない。§1.2 の「主張しないこと」に
1 行追加する性質のものである。

**M1 の基準自体が甘くない**点に注意。1.5 倍という係数が厳しすぎたのではないかという逃げ道は、
診断 2 が塞いでいる。基準を緩めても、素朴な混合器に負けている事実は変わらない。

**評価 $T$ の扱いと「つまみの分解能」**（2026-08-04 追記）。上の判定は評価 $T=2048$ で行ったが、
これは**測定のための値であって動作点ではない**。学習も実運用も $T=48$ である。$T$ を上げる必要が
あったのは M3 だけで、M1 は $T=256$ と $2048$ でほぼ不変（0.0147 → 0.0146）、
**M2 は $T=48$ で完全に同一の結果**（Spearman 1.000、21 点すべて相異なる）だった。
主張を支える指標は実用的な $T$ で成立している。

そのうえで、単一前向き計算で行動する主体にとって意味があるのは「**つまみを何段階に読み分けられるか**」
である。1 回の評価の不確かさ（同一 $\lambda$ で 2 回引いた距離）を、軸全体での出力の移動距離
$\lVert y(1)-y(0)\rVert$ で割ると、分解できる最小の $\Delta\lambda$ が出る:

| 評価 $T$ | 分解能 $\Delta\lambda$ | 区別できる段数 |
|---|---|---|
| 48（学習・動作点） | 0.177 | 約 6 |
| 100 | 0.125 | 約 8 |
| 256 | 0.078 | 約 13 |
| 2048 | 0.028 | 約 36 |

$1/\sqrt{T}$ に正確に乗る（48→100 で 1.42 倍、100→256 で 1.60 倍、256→2048 で 2.79 倍。
理論値 1.44 / 1.60 / 2.83）。**動作点 $T=48$ でも約 6 段**で、スイッチが与える 2 段より明確に多い。
つまり非現実的な $T$ に頼らなくても「離散スイッチではない」は言える。ただし $n_\lambda=21$
（$\Delta\lambda=0.05$）という細かい掃引は動作点では分解できないので、**M3 のような
隣接ステップに依存する指標は、掃引を粗くするか $T$ を上げるかのどちらかが要る**。
隣接ステップで分解能を測ってはいけない: 低 $T$ ではステップがほぼ jitter そのものになり、
**ノイズが増えるほどつまみが細かく見える**という逆向きの偏りが出る。

**次に試すなら**（未実施、優先度は高くない）:

- **中間場を学習に含める**と精度が回復するか。回復するなら「補間できる」ではなく
  「補間を学習できる」という別の（弱い）主張になるので、L4 の主張としては使えない。
  ただし機構の理解には効く。
- **$\alpha_\text{mix}$ 依存**。既定の近似 one-hot では目標が入力次元ごとに分解するので、
  内点誤差はもっと小さいはずである。もしそうなら「L4 は課題の分解可能性に依存する」という、
  §7.3 の L2 と同型の知見になる（ネットは再学習が必要で 3 seed で約 3.5 時間）。
- **行動レベル（M4）**。`world.rollout` を $\lambda$ 固定の場で回す。§7.1 の教訓どおり行動指標は
  飽和・雑音が強く、量的部分が既に落ちている以上、得るものは少ない。

> **コストの実測**（2026-08-04）: sample・単層・64 ユニット・グリッド 21² の学習は
> **約 1.66 秒/エポック**で、§7.9.1 の 1 秒/エポックより重い。3 seed × 2500 エポックで約 3.5 時間。
> 評価は学習と独立に $T$ を上げられる（`--eval-samples`）ので、$\lambda$ 掃引と再判定は無料に近い。
> ただし**評価 $T$ を上げるのは測定の都合であって、動作点の主張ではない**（上の分解能の表）。
> 隣接ステップに依存する指標（M3）だけは $T=256$ で「雑音だから合格」する状態にあり、
> $T=2048$ で初めて判定可能になった。主張を支える M1・M2 は $T=48$ で同じ結論になる。

---

### 7.12 知覚の再設計 — セクター知覚と壁チャネル（Stage 1、2026-08-07）

`neuromod/world.py`（`SENSING = "vector" | "sector"`）、駆動は `tmp/neuromod_behavior_modes.py`
（`--sensing sector` が既定。`neuromod_behavior_sr.py` ほか既存スクリプトは vector のまま＝記録済み結果は不変）。

**動機**。従来の 6D 入力（カテゴリ別・最近接オブジェクトへの生の相対ベクトル）には、主張に関わる
構造的欠陥が三つある。

1. **多重化がほぼ自明になる**。行動 $s$ の教師則 $z=\alpha_f r_\text{food}-\alpha_t r_\text{threat}
   +\alpha_s r_\text{shelter}$ は観測の**ほぼ線形関数**であり（tanh 頭打ちのみ非線形）、しかも
   近似 one-hot の $\alpha$ では各行動が**互いに素な 2 次元ブロック**だけを読む。§4・§7.3 の
   near-one-hot 批判の入力版である。実測がこれを裏づける：64 ユニットで MSE $2\times10^{-5}$ まで
   落ちる（下表）。「1 つのネットワークに 3 行動」の実質は、この符号化の下では薄い。
2. **壁が知覚できない**。閉ループの不自然さ（壁張り付き・シェルター入口での滞留・food 封鎖）への
   対処が、教師側の壁整合変換や調停ルールへ漏出し続けた（§7.12a）。
3. **非有界**。遠方ほど大きな入力になり、交差ユニットの動作点 $h/\sigma$ の議論と相性が悪い。

**定式化**。方位を $K$ 分割し（$\varphi_k=2\pi k/K$）、チャネル $c\in\{\text{food},\text{threat},
\text{shelter},\text{wall}\}$ ごとに有界近接度を返す。エージェント位置 $p$、カテゴリ $c$ の
オブジェクト $o_j$（相対ベクトル $r_j=o_j-p$、$d_j=\lVert r_j\rVert$、$\theta_j=\operatorname{atan2}(r_j)$）に対し

$$
s_{c,k}(p)\;=\;\max_{j\in\text{avail}(c)}\; w_k(\theta_j)\, e^{-d_j/\lambda},\qquad
w_k(\theta)\;=\;\cos^2\!\Big(\frac{K(\theta-\varphi_k)}{4}\Big)\;
\mathbb 1\big[|\theta-\varphi_k|\le \tfrac{2\pi}{K}\big]
$$

$w_k$ は隣接セクターと半値で重なる raised cosine で、$\sum_k w_k(\theta)=1$（1 の分割）。
オブジェクトが方位を掃くとき符号は滑らかに遷移する。$\max_j$ は総和と違い**重心アトラクタ**
（2 物体の平均方向に幻の目標が立つ）を作らない。壁チャネルは方向 $u_k=(\cos\varphi_k,\sin\varphi_k)$
のレイと $[-1,1]^2$ 境界の距離 $t_k(p)$ で

$$
s_{\text{wall},k}(p)=e^{-t_k(p)/\lambda_w},\qquad
t_k(p)=\min_{a:\,u_{k,a}\neq0}\frac{\operatorname{sign}(u_{k,a})-p_a}{u_{k,a}}
$$

入力は $x\in[0,1]^{4K}$。既定 $K=8$（32 次元）、$\lambda=2.0$、$\lambda_w=0.25$。**入力は完全に
相対知覚（自機原点）であり、絶対位置は含まれない**——$p$ はシミュレータが「センサが何を読むか」を
計算するためだけに現れる（壁チャネルも自機からのレイ距離＝知覚可能な壁までの相対距離であって
GPS ではない）。方位軸はワールド固定（コンパス方位）で、体軸化（回転不変）は heading 状態と
極座標出力（Stage 3）とセットになる。**内受容（hunger 等）は入力に入れない**——drive は
ノイズ場の選択・混合を介してのみ作用するというアーキテクチャの核はそのまま（§1.1）。教師は幾何から直接計算する
`world.make_behavior_targets`（規則は従来と同一の $v=\tanh(\gamma\lVert z\rVert)\,\hat z$）に、
壁ソフト反射（margin 0.15 内で外向き成分を smoothstep で $-\kappa$ 倍へ、$\kappa=1$）を**教師則の
一部として**内蔵した。純減衰（$\kappa=0$）は帯域が粘性化して**無処置より悪化**する実測がある
（壁帯域滞在 7.1%→23.9%）ので $\kappa\approx1$ を守ること。

**実測比較**（seed 7、analytic、調停は §7.12a の既定：threat_range 0.5、smoothing 0.2、
refuge 0.30、risk_hunger 0.9。閉ループは 4 エピソード × 1500 フレーム）:

| 構成 | 学習 MSE（f/t/s） | foods/1k | wall85 / wall95 | $d_{\min}$ | d<0.3 | 最長無採餌 |
|---|---|---|---|---|---|---|
| vector 6D・64u・31²・3000ep | 2e-5 / 9e-4 / 3e-5 | **8.8** | **2.5% / 0.1%** | 0.048 | 19.6% | 217f |
| sector 32D・64u・31²・$\lambda{=}1$ | 8e-4 / 1.5e-3 / 7e-4 | 6.2 | 35.0% / 4.7% | 0.044 | 7.9% | 208f |
| sector 32D・144u・61²・$\lambda{=}2$・5000ep | 5e-4 / 1.4e-3 / 2e-4 | 6.3 | 11.0% / 2.1% | 0.004 | 19.8% | 368f |

診断の連鎖（`tmp/` の scratch、数値は上表と同一プロトコル）: $\lambda$ を 1→2 に上げても
壁帯域滞在はほぼ不変（35→31%）→ 61² 訓練グリッドでも不変 → 開ループ診断で**壁帯域のフィットは
正確**（外向き成分 予測 0.641 vs 教師 0.618）だが**内部領域の密グリッド汎化が 20–600 倍悪い**
ことを特定 → 容量を 64→144 ユニットに上げて壁帯域滞在 11% まで回復、という順である。

**解釈**。(i) vector の MSE $10^{-5}$ は教師の線形性の直接証拠であり、「多重化がほぼ自明」という
§4 批判の定量化になっている。(ii) sector で誤差が 1–2 桁上がり容量を要求するのは退行ではなく、
**共有基盤上の多重化が初めて非線形な実タスクになった**帰結である。3 行動は同じ 32 次元を読み、
分業は入力次元の分割では実現できない。(iii) ただし行動の磨き（内部操舵精度：$d_{\min}$ が
0.004 まで落ちる、壁帯域 11% は vector の 2.5% より悪い）は未了で、候補は容量・minibatch 学習
（`protocol.train(chunk=...)`）・$\max\to$ softmax の平滑化・$K,\lambda$ の再調整。

**指標側の穴（重要）**。参加度の重なり（交差率 $\nu$ サポートの Jaccard、§7.3）は**ノイズ場の
幾何だけで決まる**ため、両符号化で完全に同一の値を返す（0.227/0.227/0.182）。つまりこの指標は
入力基盤の共有を**測れていない**。Stage 1 の共有は入力層の構造として成立しているが、その定量化には
第 1 層重みの行動間共有度（例：状態別の実効入力重みの重なり）のような別の指標が要る。§8(h)。

#### 7.12a 閉ループ調停の 2 ゲート（同日、Stage 2 への前段）

行動の不自然さのうち知覚では説明できない 2 件は、調停（`neuromod_weights`）の欠陥だった。
いずれも default-off の引数として実装し、modes ドライバのみ既定で有効化：

- **refuge ゲート**（`refuge_range=0.30`）: shelter 目標中、threat の緊急度を最寄りシェルターへの
  近さで減衰（$a\mathrel{*}=1-e^{-(d_\text{sh}/0.3)^2}$）。徘徊する threat が入口に居座ると flee 混合が
  弱い到着側駆動を打ち消し、**1 回の接近で 336 フレーム**入口を周回していた（→ 9–14f に短縮）。
- **risk ゲート**（`risk_hunger=0.9`）: foraging 中、hunger>0.6 から smoothstep で threat 緊急度を
  最大 0.9 減衰（飢餓–捕食リスクのトレードオフ）。threat の徘徊箱は 2 つの food を両方含むため、
  居座りが**全選択肢を同時封鎖**し、無採餌 foraging の約 8 割を占めていた（最悪 2891f → 自壊的に
  110–260f）。線形ゲート（onset なし）は満腹時まで大胆になり $d_{\min}$ 0.02 まで悪化するので不可。

この 2 ゲートは「drive が threat チャネルをゲートする」という同型の変換であり、goal ビット・
概日的な長周期切替とともに、**3 つのモジュレータ濃度の力学（漏れ積分器＋相互作用）へ統合するのが
Stage 2** の計画である。if 文の集積を修飾物質間相互作用の言葉に置き換える。

#### 7.12b セクターの不自然行動の原因は「純粋場でしか訓練していない」こと（2026-08-07）

§7.12 の残課題 (g) の解決。sector 移行直後の閉ループには意味不明の行動が残っていた：
forage 中に近い food へ向かわない／近づく threat へ自走する／sheltering が一度も発現しない。

**診断の連鎖**が要点である。(i) 純粋場での学習済みベクトル場の方向誤差は密グリッドで
**平均 1–2°**＝開ループはほぼ完璧。(ii) しかし閉ループは**常に混合場**で走る
（`neuromod_weights` は goal 場と threat 場の凸結合しか出さず、threat_range 0.5 では
$d_\text{threat}=0.5$ でも $w_\text{threat}\approx0.63$）。(iii) §7.11 の実測どおり、純粋場だけで
学習したネットは混合場の下で素朴な出力混合器より悪い。vector 符号ではタスクがほぼ線形なので
混合が良性だったが、**sector で非線形になった途端この弱点が行動を破壊した**。(iv) さらに学習は
threat が scripted 固定配置の 1 シーンのみで、実行時の徘徊 threat の配置分布を見ていない
（非線形写像はチャネル間で分解しないため、未見の配置で崩れる。$d_{\min}$ が 0.006 まで落ちて
threat をすり抜けるのが症状）。sheltering 不発現は独立の症状ではなく採餌失敗の帰結
（hunger が 0.35 に届かず shelter goal が発火しない）。

**対処＝実行時分布での学習** `protocol.train_blended`。各ステップで、ランダム位置ミニバッチ
（固定グリッドはセクター符号では格子間過学習する）× ランダム混合 $w$（30% 純粋／50%
goal–threat 辺／20% 単体内部 Dirichlet）× **threat 配置のランダム化**（徘徊箱
$[-0.72,0.72]^2$、shelter keepout 尊重）を標本化し、同じ幾何規則の教師で回帰する。
既定 40k steps・bs 512・pool 16384。

**結果**（4 エピソード × 1500f、症状直結の指標。appr_cos = 「goal=food・$d_f<0.5$・$d_t>0.5$」
での進行方向と food 方向の平均コサイン、toward = 「$d_t<0.4$・flee 賦活中」に threat へ向かう
フレーム率、rest = sheltering 完遂数）:

| 構成 | appr_cos | toward | rest | foods/1k | $d_{\min}$ | wall85/95 |
|---|---|---|---|---|---|---|
| vector 基準（64u） | +0.32 | 28.4% | 12 | 9.0 | 0.025 | 0.9 / 0.8% |
| sector 純粋場学習（144u） | +0.19 | 35.0% | 6 | 6.7 | 0.006 | 12.2 / 1.6% |
| **sector 混合+乱配置学習** | **+0.36** | **22.2%** | **17** | **8.8** | **0.024** | 14.4 / **1.1%** |
| （参考）教師則オラクル | — | — | 17 | 8.8 | 0.050 | 8.6 / 1.0% |

3 症状はすべて解消し、行動指標は vector 同等〜以上に達した。**オラクル行が重要**：教師＋調停
自体が壁帯域に 8.6% 滞在するので、これがこの教師で到達可能なフロアである（vector の 0.9% は
教師からの偶然の逸脱＝準線形平滑化の副産物であって、目標値ではない）。残差は帯域滞在
14.4% vs 8.6% のみ。純粋場の方向誤差は 7.6° に上がるが（容量が配置汎化に回る）、行動には
表れない。

**注意（L4 との関係）**。この学習法のネットは混合場を**見て**いるので、§7.11 のゼロショット
補間主張には使えない。あの主張は純粋場学習ネットで確立済みであり、そのまま残る。デモは
「自然な適応行動の生成」が目的なので混合を学習に含めることは正当だが、原稿では両者を
混同しないこと。`--train-mode pure` で従来プロトコルは常に再現できる。

**追補（同日）：食後の停止と food 可用性のランダム化**。残っていた最後の症状は「1 個目を
食べた直後、すぐ近くの 2 個目の**手前で停止**する」だった。診断：学習は food が**常に全部ある**
分布だったため、「自分が立っている food が消えた」観測をネットは一度も見ていない。開ループの
プローブ（food1 周辺 200 点）では、食後レジームでも**方向は完璧**（cos(pred, →food2)=+0.96）だが
**速さが教師の半分**（$|v|$ 0.41 vs 0.88）——food1 近傍で学習した減速帯の低速度にアンカーされる。
しかも停止すると food1 の respawn 条件（距離 0.25 超）が満たされず**自己固定**する。グローバル
位置の混入ではない（入力は上記のとおり純相対）。対処：`train_blended` で各ステップ独立に
各 food を $p=0.2$ で落とし、知覚と教師に同じマスクを適用（`randomize_food`、既定 on）。
結果：食後プローブ $|v|$ 0.41→**0.73**（cos +1.00）、閉ループの food 手前停止
（goal=food・$0.12<d_f<0.5$・$|v|<0.05$）**15.0→8.2/1k**、副次効果として foods/1k
8.8→**9.2**（vector 超え）、$d_{\min}$ 0.024→**0.085**、wall95 0.3%。教訓は §7.12b 本文と同じ
一般形で、**「閉ループが訪れる状態分布はすべて学習分布に入れる」**——混合 $w$・threat 配置・
food 可用性の 3 つで初めて揃った。

---

### 7.13 標準ベンチマークでの SR 網羅調査（2026-08-08 実施、08-09 平易化改稿）

> **位置づけ（2026-08-09 更新）**: **実験 8 ＝ P3 の一次証拠**（2 値マスク訓練 ×
> 連続テスト; 自明性を構成で除去）。**実験 1 ＝ P1 デモの行動版**（閉ループ）。
> **実験 2–3 ＝ 補足**（単峰性の監査; 査読防御の控え）。**実験 4–7 ＝ 別論文候補**
> （耐性・恒常性適応; 主ロジックには組み込まない）。

すべて sector 感覚＋sample モデル＋標準閉ループ（付録 C）、教師あり学習 40k ステップ。
データ: `tmp/out/sr_standard/*.csv`、図: 同 `fig_acute/chronic/grid.png`、スクリプト:
`tmp/neuromod_sr_standard.py`（旧 vector 系 SR スクリプトは破棄・置換）。

**評価量**（全実験共通）: (a) 行動——3 昼夜（1260 フレーム）の閉ループで、採餌数
（foods/1000fr）と帰巣率（夜のうちに巣に着けた夜の割合）。(b) 回帰誤差——61×61
グリッドでの教師ベクトル場との MSE（task_err）。補助として signal / separation（§C.9）。

**実験 1（評価時の強さだけ変える; 旧称 E1・急性）** 学習後に、ノイズ場全体に掛ける
倍率 $c$ を 0.05–4 で掃引（学習時は $c{=}1$）。5 seed × 13 点。
→ 性能は $c{=}1$ 付近が最良の山型。**学習時の 2 割以下（$c\le0.2$）で採餌・帰巣が
完全にゼロ**（全 seed）。4 倍で採餌半減。

**実験 2（学習時の強さを変え、毎回ゼロから学習し直す; 旧称 E2・慢性）** 学習時の
場の強さ（ピーク値）を 0.1–4.0 の 9 水準にし、各水準で新規学習・自水準で評価。
3 seed。→ **0.2–4.0 は横一線**（採餌 ≈7.0、帰巣 ≈1.0、task_err ≈0.044）。劣化するのは
0.1 のみ（採餌 4.4）。つまり「最適なノイズ強度」は学習し直せる限り存在しない——
ネットは重みスケールで任意の強さに合わせ込む（§5.1 のゲージ論の予言どおり）。

**実験 3（両者の掛け合わせ; 2 次元）** 学習時の強さ 5 水準 {0.2, 0.4, 0.8, 1.6, 3.2}
× 評価倍率 $c$ 9 点 × 3 seed。結果は 3 つ:

1. どの行でも task_err 最小は**倍率ちょうど 1**（自分が学習した強さ）。最適点は
   16 倍のレンジで学習水準と一緒に動く。
2. 行動全滅の境界も**倍率 0.1–0.2** とどの行でも同じ。絶対的な強さでは決まらない
   （強さ 3.2 で学習した網は絶対強度 0.32 で全滅するが、0.2 で学習した網は
   その強度で普通に機能する）。
3. 全曲線を「評価時の強さ ÷ 学習時の強さ」を横軸に描き直すと**ほぼ 1 本に重なる**
   （`fig_grid.png` 左）。例外は両端のみ: 学習強さ 0.1–0.2 では合わせ込み不全
   （適応の絶対的下限）、評価強度 10 超で余分の脆化。そして $\sigma{=}0$ は交差活性が
   恒等ゼロのため**どんな重みでも機能しない**（解析的事実・適応不変）。

**結論——L3「資源性」の言い換え。** 「ノイズに魔法の最適値がある」は言えない
（実験 2 が棄却; 旧 vector 設定の学習水準内点最適は学習不足の産物だった可能性が高く、
再現しない。§6 冒頭の注記参照）。言えるのは次の 3 つで、L3 はこの形に組み替える:

1. **急に**強さを変えられると壊れる。弱める方向は学習時の 2 割で行動が全滅する
   （逆 U 字の実体）。
2. **ゆっくりなら**（学習し直せるなら）どんな強さにも適応でき、最適点ごと移る——
   薬理学の**耐性**と同じ構図。急性の逆 U 字と慢性の耐性が同じ機構から同時に出る。
3. **ゼロだけは絶対に不可能**。ノイズなしでは計算が存在しない（動員ゲートの核）。

**実験 8（P3 の一次証拠: 2 値マスク訓練 × 連続テスト、2026-08-09）**
`tmp/neuromod_sr_pure.py`、`pure_conc_seed*.csv` / `pure_interp_seed*.csv`、
図 `fig_pure.png`。実験 1 の自明性問題（山が訓練点に一致）を**構成で**除去する:
訓練は 3 枚の**2 値場**（各ユニットは 0 か σ₀ のみ; バンプを台で 2 値化）の純粋
コーナーのみ（開ループ回帰、grid 61、4000 epochs）。ユニットごとに訓練された
ノイズ水準は {0, σ₀} の**2 点だけ**。テストで連続軸を掃引する（すべて未訓練の値）:

- **濃度掃引**: 滑らかな単峰カーブ（signal 0.28→ピーク 0.86 @ c≈0.85–1.0 →0.46;
  task_err 最小 0.007、沈黙側 0.275、飽和側 0.158）。seed 分散は ±0.001–0.02 と
  極小。**形と連続性は訓練分布から出所を説明できず、交差物理に帰属する**
  （山の位置のみ訓練で説明可能——それは認める）。
- **場の補間**（2 値コーナー間、内部は未訓練）: 含意混合率 $\hat\lambda$ が w に
  ほぼ恒等で追従（**9 曲線すべて Spearman 1.000**、$\hat\lambda(0.5)=0.45$–$0.57$）。
  ゼロショット補間の法則性が sector＋2 値でこれまでで最も綺麗に成立。
  量的な内部精度は端点の約 6.6 倍の誤差（0.046 vs 0.007）で §7.11 と同率——
  **量的主張はしない**（L4 の従来方針を維持）。

**論文上の役割**: P3 の一次証拠はこの実験に置く（閉ループの実験 1 は P1 デモの
行動版として保持）。訓練時の場が 2 値＝純粋な**動員マスク**であることは、台帳④の
「行動状態の動員パターン」という語彙とも一致し、P1 の言い方も純化する。

**2 値マスクの生物学的身分（2026-08-09 追記）**: 2 値なのは**感受性（受容体発現）**の
層であり、それは生物でも細胞型レベルでほぼ離散的である（D1/D2 MSN のほぼ排他的な
発現、5-HT3A を持つ介在ニューロン亜集団）。STG でも各修飾物質は**特徴的な標的
サブセット**の細胞に作用する（複数のペプチドが、異なるが重なり合う細胞サブセットの
同一電流に収束する; Swensen–Marder 系、§9）。濃度の層は連続のまま（テスト軸 c, w）。
避けるべきは 2 値性ではなく**分割（disjoint）**であり、2 値化は元のバンプの台を
そのまま使うため重なり構造は不変（各場 60–64 ユニット中ペアごとに 23–24 共有、
Jaccard ≈ 0.23）——純粋コーナー訓練では共有ユニットが複数行動の訓練を受けるので
多重化圧力はむしろ純粋に残る。総括すると、2 値化は生物についての主張ではなく
**測定プロトコル**（訓練集合の有限化）であり、生物の読み（P2）は連続場のまま。

**graded 対照（同一プロトコル・場だけ元のバンプ; `pure_graded_*.csv`,
`fig_pure_control.png`）**: 単峰カーブの形・ピーク位置・ピーク値は一致
（signal 0.862 vs 0.861、err 0.006 vs 0.007 @ c≈1）。補間の法則性も同一
（Spearman 全曲線 1.000、$\hat\lambda(0.5)=0.51$、内部誤差 0.052 vs 0.047）。
腕の傾きのみ二次的に異なる（graded は低濃度側でより急峻・高濃度側でより緩やか——
縁の小 σ ユニットの寄与として説明可能）。**2 値化が結果を作っているのではない**こと、
すなわち主張のすべての担い手が両プロトコルで再現されることを確認した。

**実験 4（耐性の自由度; E6、2026-08-09 実施）** 0.8 で学習した網（3 seed）の場を
α∈{0.25, 0.5, 2, 4} 倍へ恒久シフトし、**全 25,920 重みを凍結**したまま少数の乗算
ゲインだけで適応させた（`tmp/neuromod_tolerance.py`、
`tmp/out/sr_standard/tolerance.csv`）。参照値（適応点）: foods 6.97 / 帰巣 1.00 /
err 0.044。結果:

| α | 無適応（急性） | 局所則 g₁=α（学習なし） | 層ゲイン 3 個を学習 |
|---|---|---|---|
| 0.25 | 0.49 / 0.87 | 0.22 / 0.26（悪化!） | **6.79 / 1.00** |
| 0.5 | 6.44 / 1.00 | 6.22 / 0.94 | 6.97 / 1.00 |
| 2 | 6.83 / 1.00 | 6.97 / 1.00 | 6.88 / 1.00 |
| 4 | 3.70 / 0.89 | **7.05 / 0.98** | 6.97 / 1.00 |

3 つの発見: (1) **層ごとのゲイン 3 個で 16 倍レンジ全域の耐性が成立**——実験 2 の
「学習し直せば吸収」に必要な自由度は乗算スケーリングで足りる（容量の意味で条件①
クリア。Turrigiano の乗算的スケーリングと機構レベルで整合）。(2) 課題信号なしの
局所則（第 1 層シナプスをトーンに比例スケール）は**増加側では完全に機能を復元する**
が、減少側では無適応より悪い。理由は h が固定のため: 第 1 層を α 倍に縮めると実効
閾値が h/α に上がり、閾値下へ深く沈む。実際に学習が見つけた解は g₁≈0.7（縮めない）
＋ **g₂≈7–8（下流の増幅）**だった——トーン低下の補償は入力側でなく下流ゲインで行う。
(3) ユニット別ゲイン（289 個）は容量的には十分（scalar3 解を含む部分空間; scalar3 解
から warm-start すると err 0.084 で 3 個版を上回る）が、極端な減少側では cold-start の
最適化が「沈黙局所解」（g₃→0 で平均を出力）に落ちる。適応は容量だけでなく力学の
問題でもある、という正直な注記を残す。

**実験 5（教師なし恒常性則; E7、2026-08-09 実施）** E6 の間隙（減少側の解の発見に
課題誤差を使った）を埋める試み。正解ラベルを一切使わず、「各ユニットの平均交差率
$\nu_k$ を適応時の設定値に戻すようユニット別ゲインを回す」発火率ホメオスタシスの
対応則で適応させた（`tmp/neuromod_homeostat.py`、`homeostat.csv`。使う信号は感覚
経験と自己の活動統計のみ）。結果（3 seed 平均、参照 6.97/1.00）:

| α | 0.25 | 0.5 | 2 | 4 |
|---|---|---|---|---|
| foods / home | **0.00 / 0.00** | 2.07 / 0.31 | 6.97 / 1.00 | **7.05 / 1.00** |

**増加側は教師なし恒常性則で完全に閉じた**（E6 の決め打ち則より強い、真の適応則）。
**減少側は届かない**——E6 で解の存在は示されている（g₁≈0.7, g₂≈7–8）が、ν 設定値則は
別の点（g₁≈0.1–0.3）に収束し機能を回復しない。平均交差率という 1 次統計の復元は、
入力依存成分（signal）の復元と減少側では一致しないため。

**解釈——非対称は生物学と整合する。** 減少側の補償に本質的に必要なのは実効閾値の
引き下げ（層 1 の厳密補償は $g\,a$ の形では書けず、閾値/バイアスの移動を要する:
実験 4 の注記）。生物側でも、トーン**喪失**からの回復（STG の除神経後回復）は
シナプススケーリングではなく**内在的興奮性（コンダクタンス＝閾値側）の再調整**で
起きる。つまり「増加への耐性はスケーリングで安価に、喪失への耐性は興奮性の再調整で」
という分業は、モデルの発見と STG の実験事実が同じ形をしている。検証は E8（下記）。: 急性停止／慢性回復の解離には、鎖 1 の
土台と同じ標本に先例がある——STG の除神経実験（修飾入力の切断でリズムが急性停止し、
数時間〜数日のコンダクタンス調整で修飾物質なしに回復; §9 耐性の錨）。受容体可塑性
（5-HT1A 脱感作、D2 過感受性）と Turrigiano の乗算的シナプススケーリングが、
実験 2 の「重みスケールによる吸収」の生物対応。注意: モデルの $\sigma{=}0$ 不可能性を
「修飾物質ゼロ」と読まないこと——生物では修飾物質非依存の背景入力の床が残る
（$\sigma$ は総ゆらぎ、修飾物質はその制御者。鎖 2 の語法を維持）。説明対象は
**回路レベルの耐性**に限定し、代謝性耐性などは範囲外と明記する。

「学習条件から離せば悪くなるのは当然では」という自明性の疑義（マッチポンプ監査）への
答えもこの形に含まれる: 山の位置は確かに自明に学習が決める（1.）。非自明なのは、
曲線全体が比率だけで決まること（2. の不変性）と、ゼロの不可能性（3.）である。
平均場対照（analytic; 記録のみ、論文では不使用）は低倍率でもゼロに落ちず機能が残る
——ゼロへの崖は交差活性の閾値障壁に固有。

---

## 8. 残作業 — このロジックで論文を完成させるまで（2026-08-08 全面更新）

ロジック（§1.1a–c）と標準ベンチマーク（付録 C）は確定。残りは「実験の再統一と
仕上げ」「執筆」「文献確認」の 3 系統に整理し直す。旧項目は末尾に対応を記す。

### 8.1 実験（優先順）

- **(E1) 行動レベル SR【完了 2026-08-08 → §7.13 実験 1】**: 5 seed で逆 U 字確立。
  アブストは改訂済み（§1.1c）。
- **(E2) SR 曲線の標準ベンチマーク再統一【完了 2026-08-08 → §7.13 実験 2–3】**:
  vector 結果は破棄。学習し直すと強さ 0.2–4.0 で性能は横一線＝「最適なノイズ強度」は
  学習が作るもので物理定数ではない。L3 は §7.13 の結論 3 点（急変に脆い / 適応で
  最適点ごと移る＝耐性 / ゼロは不可能）に組み替え（§6 の注記参照）。
- **(E3) $\alpha_{\text{mix}}$ の整合**: L2（損傷）は $\alpha_{\text{mix}}>0$ が成立条件
  （§7.3）だが標準デモは one-hot 既定。論文内で「デモは one-hot・多重化検証は
  mix=0.3」と使い分けを明記するか、標準を mix>0 に寄せて行動品質を再測定するか
  （旧(f)）。
- **(E4) 行動デモの図**: 概日エソグラム（goal・行動ラベル・摂食・帰巣の時系列）＋
  食性/巣の多様性図（C.9 の均等度）。GIF は補足資料。
- **(E6) スケーリングのみの耐性【完了 2026-08-09 → §7.13 実験 4】**: 層ゲイン 3 個で
  16 倍レンジ全域の耐性が成立。条件①クリア。増加側は課題信号なしの局所則でも十分。
- **(E7) 交差率セットポイント則【完了 2026-08-09 → §7.13 実験 5】**: 増加側は
  教師なしで完全復元。減少側は不到達（解は存在するが ν 設定値則では見つからない）。
- **(E8) 内在的興奮性の適応【完了 2026-08-09 → §7.13 実験 6】**: 3 種の教師なし則
  すべて減少側で否定。自由重み対照は記憶消去まで実証（定数倍制限の意味が確定）。
  減少側の則レベル閉包は明示的な未解決問題として論文に記載。
- **(E9) ランプ vs ステップ【完了 2026-08-09 → §7.13 実験 7】**: 3 seed 全一致。
  急ステップは適応なしで恒久劣化、恒常性則で完全回復、緩ランプ＋則は無停止。
  「急なら壊れ、緩やかなら気づかれない」を 1 個体の実時間で実証。
- **(E5) 同一重み診断（旧(d)、優先度低）**: 各モデル別学習は厳密な同一重み比較では
  ない。リザバー文脈の実装（§6）を流用可能。

### 8.2 執筆

- **(W1) Methods**: 付録 C.0–C.10 をほぼ直訳で英語化（C.0 の導出が課題設定の段落）。
- **(W2) Introduction**: §1.1a の三つの鎖を骨格に、§2–§3 から引用を配置。
- **(W3) 対応表**: 台帳（§1.1b）を「モデル要素 ↔ 生物対応 ↔ 主張しないこと」の
  3 列表として本文か補足に置く。査読防御の中核。
- **(W4) アブスト**: §1.1c で確定。E1 が立てば行動水準の一文を追加。
- **(W5) 語彙統一**: 「neuromodulator-like」「recruitment」「participation (ν)」
  「quasi-static concentration field」。food field 等のカテゴリ命名は使わない
  （台帳④）。

### 8.3 文献・実装

- **(R1) 文献の一次確認（旧(a)、拡張）**: §9 は全て記憶からの引用。骨格 5 本
  （Doya 2002 / Servan-Schreiber 1990 / Marder 2012 / McGinley 2015 /
  Ölveczky 2005）に加え、今回の追加分（Gidon 2020 / Rice & Cragg 2008 /
  パドルフィッシュ Russell 1999 / Padilla 2016 / Burnett 2016 / Chance 2002 /
  Destexhe 2003 / Anderson 2000）と神経支配地形の一次資料を当たる。
- **(R2) コード配置（旧(e)）**: 公開するなら `tmp/neuromod*` → `examples/` へ。
- **(R3) 入力共有の定量指標（旧(h)、優先度低）**: ν-Jaccard は場の幾何で決まり
  符号化に不感。第 1 層実効重みの共有度など。

### 旧項目との対応

(a)→R1 / (b)→E2 / (c) L4: 解決済み・質的部分のみ主張 / (d)→E5 / (e)→R2 /
(f)→E3 / (g) 行動品質は解決済み（§7.12b）、SR 系の sector 移行は E2 /
(h)→R3 / (i) 調停 ODE 化: 概日時計の導入（付録 C.8）で主要部は解消。refuge /
risk / panic の漏れ積分器化は任意の拡張であり、論文には不要。

---

## 9. 文献（すべて未確認。§8(a) 参照）

**枠組み**
- Doya K (2002) Metalearning and neuromodulation. *Neural Networks* 15:495–506.
- Doya K (2008) Modulators of decision making. *Nat Neurosci* 11:410–416.
- Bargmann CI (2012) Beyond the connectome: how neuromodulators shape neural circuits. *BioEssays* 34:458–465.

**ゲイン・SNR・容積伝達**
- Servan-Schreiber D, Printz H, Cohen JD (1990) A network model of catecholamine effects: gain, signal-to-noise ratio, and behavior. *Science* 249:892–895.
- Salinas E, Thier P (2000) Gain modulation: a major computational principle. *Neuron* 27:15–21.
- Ferguson KA, Cardin JA (2020) Mechanisms underlying gain modulation in the cortex. *Nat Rev Neurosci* 21:80–92.
- Agnati LF, Fuxe K ら, volume transmission（一次資料を確認すること）。

**不確実性・探索・符号化**
- Yu AJ, Dayan P (2005) Uncertainty, neuromodulation, and attention. *Neuron* 46:681–692.
- Aston-Jones G, Cohen JD (2005) An integrative theory of locus coeruleus-norepinephrine function. *Annu Rev Neurosci* 28:403–450.
- Hasselmo ME (2006) The role of acetylcholine in learning and memory. *Curr Opin Neurobiol* 16:710–715.

**STG の標的サブセット・リズム間の所属移動（§7.13 実験 8 の 2 値マスクの錨）**
- Swensen AM, Marder E (2000/2001) 複数の修飾物質が異なる（重なり合う）細胞サブセットの同一電流 I_MI に収束する系列（一次資料を確認すること）。
- Weimann JM, Marder E (1994) Switching neurons are integral members of multiple oscillatory networks. *Curr Biol* 4:896–902.（ニューロンのリズム間所属移動＝重なりの実在）

**同一回路・複数出力・degeneracy**
- Marder E (2012) Neuromodulation of neuronal circuits: back to the future. *Neuron* 76:1–11.
- Marder E, Bucher D (2007) Understanding circuit dynamics using the stomatogastric nervous system. *Annu Rev Physiol* 69:291–316.
- Nusbaum MP, Beenhakker MP (2002) A small-systems approach to motor pattern generation. *Nature* 417:343–350.
- Edelman GM, Gally JA (2001) Degeneracy and complexity in biological systems. *PNAS* 98:13763–13768.
- Marder E, Taylor AL (2011) Multiple models to capture the variability in biological neurons and networks. *Nat Neurosci* 14:133–138.

**逆 U 字の実例**
- Yerkes RM, Dodson JD (1908) The relation of strength of stimulus to rapidity of habit-formation. *J Comp Neurol Psychol* 18:459–482.
- Vijayraghavan S et al. (2007) Inverted-U dopamine D1 receptor actions on prefrontal neurons engaged in working memory. *Nat Neurosci* 10:376–384.
- Cools R, D'Esposito M (2011) Inverted-U-shaped dopamine actions on human working memory and cognitive control. *Biol Psychiatry* 69:e113–125.
- McGinley MJ, David SV, McCormick DA (2015) Cortical membrane potential signature of optimal states for sensory signal detection. *Neuron* 87:179–192.

**耐性・恒常性適応の錨（§7.13 の慢性側、2026-08-09 追加）**
- Thoby-Brisson M, Simmers J (1998) Neuromodulatory inputs maintain expression of a lobster motor pattern-generating network... *J Neurosci* 18:2212–2225.（除神経で急性停止）
- Golowasch J, Casey M, Abbott LF, Marder E (1999) Network stability from activity-dependent regulation of neuronal conductances. *Neural Comput* 11:1079–1096.（回復の機構モデル）
- Luther JA et al. (2003) Episodic bouts of activity accompany recovery of rhythmic output... *J Neurophysiol* 90:2720–2730.（数日での回復）
- Turrigiano GG (2008) The self-tuning neuron: synaptic scaling of excitatory synapses. *Cell* 135:422–435.（乗算的スケーリング＝モデルの重みスケール補償の対応物）
- Blier P, de Montigny C (1994) Current advances and trends in the treatment of depression. *Trends Pharmacol Sci* 15:220–226.（5-HT1A 自己受容体脱感作＝モノアミン系の耐性）

**行動・生態側の受け皿（§1.1a の鎖 1、2026-08-08 追加）**
- Russell DF, Wilkens LA, Moss F (1999) Use of behavioural stochastic resonance by paddle fish for feeding. *Nature* 402:291–294.（行動レベル SR の直接先例）
- Fanselow MS, Lester LS (1988) A functional behavioristic approach to aversively motivated behavior: predatory imminence... （防御距離・脅威切迫度の枠組み; panic/refuge の対応先）
- Caraco T, Martindale S, Whittam TS (1980) An empirical demonstration of risk-sensitive foraging preferences. *Anim Behav* 28:820–830.（risk-sensitive foraging）
- Stephens DW, Krebs JR (1986) *Foraging Theory*. Princeton UP.
- Padilla SL et al. (2016) Agouti-related peptide neural circuits mediate adaptive behaviors in the starved state. *Nat Neurosci* 19:734–741.（空腹が恐怖応答を抑制; 勇気ゲートの実体）
- Burnett CJ et al. (2016) Hunger-driven motivational state competition. *Neuron* 92:187–201.

**ノイズ＝機能的資源**
- Ölveczky BP, Andalman AS, Fee MS (2005) Vocal experimentation in the juvenile songbird requires a basal ganglia circuit. *PLoS Biol* 3:e153.
- Kao MH, Doupe AJ, Brainard MS (2005) Contributions of an avian basal ganglia-forebrain circuit to real-time modulation of song. *Nature* 433:638–643.
- Fee MS, Goldberg JH (2011) A hypothesis for basal ganglia-dependent reinforcement learning in the songbird. *Neuroscience* 198:152–170.
- McDonnell MD, Ward LM (2011) The benefits of noise in neural systems. *Nat Rev Neurosci* 12:415–426.
- Douglass JK et al. (1993) Noise enhancement of information transfer in crayfish mechanoreceptors by stochastic resonance. *Nature* 365:337–340.
- Levin JE, Miller JP (1996) Broadband neural encoding in the cricket cercal sensory system enhanced by stochastic resonance. *Nature* 380:165–168.
- Collins JJ, Imhoff TT, Grigg P (1996) Noise-enhanced tactile sensation. *Nature* 383:770.
- Priplata AA et al. (2003) Vibrating insoles and balance control in elderly people. *Lancet* 362:1123–1124.
- Faisal AA, Selen LPJ, Wolpert DM (2008) Noise in the nervous system. *Nat Rev Neurosci* 9:292–303.

**準定常濃度場の錨（§1.1b ③、2026-08-08 追加）**
- Rice ME, Cragg SJ (2008) Dopamine spillover after quantal release: rethinking dopamine transmission... *Brain Res Rev* 58:303–313.（放出部位周囲の影響圏、取り込みが範囲と時間スケールを規定）
- 神経支配地形（層別プロファイル・ストリオソーム/マトリックス）の一次資料を要選定。

**単一ニューロンの非単調 I/O・XOR（§1.1b ②）**
- Gidon A et al. (2020) Dendritic action potentials and computation in human layer 2/3 cortical neurons. *Science* 367:83–87.（dCaAP: 閾値付近で振幅最大の非単調応答、単一細胞での XOR）

**ノイズの生理学的実体・変動性の修飾**
- Chance FS, Abbott LF, Reyes AD (2002) Gain modulation from background synaptic input. *Neuron* 35:773–782.
- Destexhe A, Rudolph M, Paré D (2003) The high-conductance state of neocortical neurons in vivo. *Nat Rev Neurosci* 4:739–751.
- Ho N, Destexhe A (2000) Synaptic background activity enhances the responsiveness of neocortical pyramidal neurons. *J Neurophysiol* 84:1488–1496.
- Shadlen MN, Newsome WT (1998) The variable discharge of cortical neurons. *J Neurosci* 18:3870–3896.
- McCormick DA (1992) Neurotransmitter actions in the thalamus and cerebral cortex. *Prog Neurobiol* 39:337–388.
- Goard M, Dan Y (2009) Basal forebrain activation enhances cortical coding of natural scenes. *Nat Neurosci* 12:1444–1449.
- Polack PO, Friedman J, Golshani P (2013) Cellular mechanisms of brain state-dependent gain modulation in visual cortex. *Nat Neurosci* 16:1331–1339.
- Minces V, Pinto L, Dan Y, Chiba AA (2017) Cholinergic shaping of neural correlations. *PNAS* 114:5725–5730.
- Cohen MR, Maunsell JHR (2009) Attention improves performance primarily by reducing interneuronal correlations. *Nat Neurosci* 12:1594–1600.

**状態依存の行動切り替え（ペプチド系）**
- Betley JN et al. (2015) Neurons for hunger and thirst transmit a negative-valence teaching signal. *Nature* 521:180–185.
- Chen Y, Lin YC, Kuo TW, Knight ZA (2015) Sensory detection of food rapidly modulates arousal-related AgRP neurons. *Cell* 160:829–841.
- Flavell SW et al. (2013) Serotonin and the neuropeptide PDF initiate and extend opposing behavioral states in C. elegans. *Cell* 154:1023–1035.
- Mobbs D et al. (2007) When fear is near: threat imminence elicits prefrontal-periaqueductal gray shifts in humans. *Science* 317:1079–1083.

**比較対象**
- Masse NY, Grant GD, Freedman DJ (2018) Alleviating catastrophic forgetting using context-dependent gating and synaptic stabilization. *PNAS* 115:E10467–E10475.
  ※ 最近傍の先行研究だが、あちらはランダム割当のゲート + 別途のシナプス安定化という**二機構**であり、
  本稿は**単一の物理量が選択と計算の両方を担う**点で異なる。詳しい対比は `idea_consolidation.md` §14.4。

---

## 付録 A. SR 曲線の再現手順（最小）

§6 の実測を再現するための操作。対象コードは
[`tmp/neuromod_sr_curve.py`](../tmp/neuromod_sr_curve.py)（行動デモ
[`tmp/neuromod_behavior_modes.py`](../tmp/neuromod_behavior_modes.py) の学習・データ・
ノイズ場をそのまま再利用する診断スクリプト）。依存は `numpy`, `torch`, `matplotlib` のみ、
実行はリポジトリ直下から、CPU 既定。

**測る量**（CSV は列順 `x, separation, signal, task_err`。1 列目名は `sigma_train` or `test_s`）:

- `separation` — 3 つのノイズ場が生む出力ベクトル場どうしの平均ペア距離（各場がどれだけ
  別々の行動を動員するか）。
- `signal` — 出力の**刺激依存成分**。SR で単峰になるべき本命量。低ノイズ端（閾値下で交差が
  起きず出力が消える）と過大（交差飽和で定数化）の両側で 0 に向かう。
- `task_err` — 学習目標への MSE（参照用）。

**R1. 厳密な SR（本命）— sample × 学習水準スイープ**
```bash
python tmp/neuromod_sr_curve.py \
    --sweep train --model sample \
    --grid-side 25 --epochs 1500 --train-steps 15 \
    --s-min 0.1 --s-max 4.0 --samples 96 --crossing-h 0.2 --seed 7 \
    --save data/sr_train_sample_seed7.csv --no-show
```
重い（sample ネットを 15 個学習）。軽量プレビューは
`--grid-side 17 --epochs 700 --train-steps 9 --samples 32`。

**R2. 対照（平均場＝SR なし）— analytic × 学習水準スイープ**
```bash
python tmp/neuromod_sr_curve.py \
    --sweep train --model analytic \
    --train-steps 15 --s-min 0.1 --s-max 4.0 --seed 7 \
    --save data/sr_train_analytic_seed7.csv --no-show
```

**R3. テスト時スイープ（高速・補助図）**
```bash
python tmp/neuromod_sr_curve.py --model sample \
    --grid-side 21 --epochs 1500 --samples 96 --s-max 4.0 --s-steps 60 --seed 7 \
    --save data/sr_test_sample_seed7.csv --no-show
```

**R4. seed 頑健性 — R1 を seed 掃引**
```bash
for s in 0 1 2 3 4; do
  python tmp/neuromod_sr_curve.py --sweep train --model sample \
    --grid-side 21 --epochs 1000 --train-steps 11 --s-min 0.1 --s-max 4.0 --samples 64 \
    --seed $s --save data/sr_train_sample_seed$s.csv --no-show
done
```

**標準出力の読み方**: `INTERIOR optimum -> SR confirmed` は signal のピークが端点でない、すなわち
SR 成立。`at an endpoint` は端点最適（analytic の train スイープで想定どおり）。

**落とし穴**

- **`--sigma` は掃引変数ではない。** `--sigma`（既定 0.22）は 8×8 ユニットシート上の**バンプの
  空間的な広がり**であり、ノイズ強度ではない。ノイズ強度は `--base-std`（test スイープ）
  または `--s-min`〜`--s-max` の掃引変数（train スイープ）。内点最適 $\sigma^*\approx0.8$–$1.2$ は
  後者の軸上の値である。
- **`--samples`（コードの `t`）は時間ではない。** 正典の $T$、すなわち 1 入力あたりの確率的
  forward サンプル数（正典 §1.1, §3.1）。時間発展・時間相関ノイズは本スクリプトに存在しない。
- **`--crossing-h` は掃引中に固定する。** SR には $h>0$ が必須で、固定こそが体制 (b) の条件
  （§6）。$\sigma$ と連動させると内点最適が定義上消える。
- **test スイープの `task_err` は交絡する。** 学習水準の近傍で最小になるのは構造上当然なので、
  主張は `separation` / `signal`（非学習目標）の内点最適か、`--sweep train` で行う。
- **SR 判定はモデル非依存ではない**（正典 §2.6）。`analytic` で内点最適が出ないのは失敗ではなく
  予測どおりの対照である。`statistic` 水準での SR の有無は**未検証**であり、
  「内点にピークが出れば SR」という読み方をそのまま適用してはいけない。

---

## 付録 B. 図の枠組み

| 図 | 使う CSV | 描く列 vs 横軸 | 主張 |
|---|---|---|---|
| **Fig 1 厳密な SR** | `sr_train_sample_seed7` | `task_err` と `signal` vs `sigma_train` | 内点最適＋低 $\sigma$ 崩壊＝真の SR（**$h=0.2$ 固定＝体制 (b)** をキャプションに明記し、$h/\sigma^*\approx0.17$–$0.25$ を併記） |
| **Fig 2 機構 vs 平均場** | `sr_train_sample_*` ＋ `sr_train_analytic_*` | `signal` vs std を重ね描き | sample = 内点、analytic = 低 $\sigma$ 端（障壁なし） |
| **Fig 3 場の分離度** | 任意の train/test CSV | `separation` vs std | ノイズ強度が場の区別度を決め最適点をもつ |
| **Fig 4 テスト時の逆 U 字** | `sr_test_sample_*` | `separation` と `signal` vs `test_s` | 1 ネットでの逆 U 字（安価な補助図） |
| **Fig 5 seed 頑健性** | `sr_train_sample_seed{0..4}` | `task_err` の mean±std vs `sigma_train` | 傾向が seed に依存しない |

本文図は Fig 1（厳密 SR）、Fig 2（機構 vs 平均場）、行動デモのパネル図（3 場 × ベクトル場）、
§7.3 の損傷図の 4 枚程度に絞る。§7.2 のベースライン依存図を Fig 1 の隣に置くと L3 が最も強くなる。

CSV は純数値なので `numpy.loadtxt(path, delimiter=',')`（`#` 行は自動スキップ）または
`pandas.read_csv(path, comment='#', header=None, names=['x','separation','signal','task_err'])`
で読める。

---

## 付録 C. ベンチマーク仕様（形式的定義・標準設定）— 唯一の規範文書

採餌 / 回避 / 帰巣ベンチマークの形式的定義。実装（`tmp/neuromod/`）と本付録を一致させて
管理し、**仕様をここ以外に書かない**。2026-08-08 の標準化時点。

### C.0 導出 — なぜ「採餌 / 回避 / 帰巣」でなければならないか

このベンチマークは選んだのではなく、主張から**導出**される。研究の新規性は
(i) 一つのネットワークの部分的賦活で行動を切り替えること、
(ii) その賦活をノイズ場が行うこと、
(iii) 神経修飾物質ダイナミクスのシンプルなモデルであること、
(iv) 行動水準で SR が生起すること、の 4 点にある。それぞれが課題に要求を課す。

**要求 1（切り替えの実在）**: 同一の感覚入力に対して**相反する写像**が複数なければ、
「切り替え」は定義できない（1 つの写像で足りてしまう）。最小の相反は同一幾何への
接近と忌避——すなわち食物への接近と脅威からの逃避で、符号が逆の応答である。

**要求 2（重なりの制御）**: 多重化の主張（共有ユニットが複数行動を担う）には、場の
**ペア重なり**が制御変数でなければならない。2 行動ではペアが 1 つしかなく重なりを
系統的に振れない。3 行動を円環上に等間隔配置すれば全ペアが等重なりになり、円環半径
1 つで重なりを掃引できる（C.5）。**3 は多重化を検証できる最小数である。**

**要求 3（内部変数の最小集合）**: 修飾物質ダイナミクスのモデルである以上、行動を
切り替える*理由*は入力ではなく**内部**になければならない。動物行動学で普遍的かつ
修飾物質の関与が最も文書化されている内的軸は 3 つ——**概日時計**（休息/活動）、
**空腹**（摂食駆動）、**脅威切迫度**（防御）。この 3 軸をそのまま行動に写すと、
昼の採餌・夜の帰巣・近接時の回避、すなわち**採餌 / 帰巣 / 回避の三行動が一意に出る**。
第 4 の行動を足す理由も、どれかを削る理由もない。

**要求 4（行動水準の SR）**: SR を回帰誤差ではなく行動で示すには、(a) 出力の大きさが
運動として可視であること（沈黙＝凍結; C.8d）、(b) 行動の成否が**数えられる**こと。
採餌は摂食数、帰巣は夜毎の到着、回避は接触の有無として数えられる——三行動はいずれも
計数可能な帰結を持つ（C.9）。

**要求 5（入力ゲートの排除）**: 切り替えがノイズ場の手柄であるためには、行動間で
**感覚基盤が共有**されていなければならない（入力の切り替えで説明できてしまうため）。
全行動が同一の方位符号を読む sector 符号（C.3）はこの要求の帰結である。

まとめると: 相反写像（要求 1）× ペア重なりの制御（要求 2）× 内的 3 軸（要求 3）×
計数可能性（要求 4）× 共有感覚（要求 5）＝ 一枚のアリーナ上の採餌 / 回避 / 帰巣。
これより単純にすると主張のどれかが検証不能になり、これより複雑にする理由はない。

### C.1 全体構造

| 記号 | 意味 |
|---|---|
| $p_\tau \in [-1,1]^2$ | 個体位置 |
| $\mathcal{O}_c$ | カテゴリ $c \in \{\text{food},\text{threat},\text{shelter}\}$ の物体集合 |
| $x_\tau \in \mathbb{R}^{32}$ | 感覚入力（C.3） |
| $\sigma \in \mathbb{R}_{\ge0}^{144}$ | 隠れ層ノイズ強度＝**神経修飾場**（C.5） |
| $w_\tau \in \Delta^2$ | 3 状態の混合重み |
| $f_\theta(x;\sigma) \in \mathbb{R}^2$ | NNN の速度指令（C.6） |

課題は 3 層の入れ子: (1) **教師つきベクトル場回帰**（報酬・方策勾配なし）、
(2) **ノイズ場によるサブネットワークのリクルート**（同一 $\theta$、$\sigma$ だけ替える）、
(3) **閉ループ調停**（時計と脅威近接が $w$ を決め、$w$ が $\sigma$ を決める。調停は運動を
計算しない — 接近・回避の幾何はすべてネットワークが担う）。

### C.2 環境

**シーン**（`make_scripted_objects`、固定）:
food $\{(0.52,-0.08), (-0.37,0.08), (0.55,-0.55)\}$、
threat $\{(0.05,-0.07), (0.42,0.52), (-0.51,-0.43)\}$、
shelter $\{(-0.40,0.52), (0.10,-0.56)\}$、初期位置は第 1 の巣（フレーム 0 ＝ 夜明け）。

配置は制約付き棄却サンプリング → 閉ループ実走で採点して選定。支配的制約は 2 つ:
(i) **全物体 $|$座標$| \le 0.6$**（外周配置は個体を境界帯に引き込み、壁滞在 0.4%→3–7%）、
(ii) **巣は周辺**（中央の巣は徘徊脅威に掃かれ帰巣率 0.03–0.61 に崩壊）。
均等な散らばりは帰巣率と交換関係にあり、現配置の帰巣 0.86 が 30 候補・4 探索戦略での上限
（コンパクト配置なら 1.00 だが視覚的に偏る）。

**脅威**: 速さ 0.01 で直進、$[-0.72,0.72]^2$ で反射。巣から $R_{\text{keep}} = 3.6 \times 0.125 = 0.45$
以内に侵入不可。1.5 倍（0.19）では脅威検知半径 0.50 の内側に脅威が座り、巣の入口で逃走と
到着が競合して帰巣が約 9% 失敗していた（引き上げで 0.914→0.992、壁・接触ゼロ）。

**食物**: $R_{\text{eat}}=0.10$ 以内で枯渇。再生は「タイマー $N_{\text{regrow}}=260$ 経過 ∧ 個体が
$2.5R_{\text{eat}}$ 以上離れている」。**再生遅延が食性多様性の本質的機構**: 距離条件のみの旧則
では再生境界を跨ぐ往復で単一食物を無限収穫でき、摂食が $[200,1,183]$、巣利用が $[5,105]$ に
潰れていた。260（昼 1 回分）で $[119,24,105]$ / $[39,71]$。

### C.3 感覚（sector 符号、標準）

$K=8$ 方位 × 4 チャネル（food, threat, shelter, wall）= 32 次元。方位中心 $\phi_k = 2\pi k/K$。

レイズドコサイン方位チューニング（隣接半値重なり・総和 1）:
$w_k(\vartheta) = \cos^2(K\Delta_k/4)$ for $|\Delta_k| \le 2\pi/K$ else $0$、
$\Delta_k = ((\vartheta-\phi_k+\pi) \bmod 2\pi) - \pi$。

物体チャネル（$\lambda=2.0$、利用可能物体のみ、**和でなく max**——和は物体群の重心に
偽の引力を作る）: $s_k^c = \max_j w_k(\vartheta_j)\, e^{-d_j/\lambda}$。

壁チャネル（$\lambda_w=0.25$）: $s_k^{\text{wall}} = e^{-t_k/\lambda_w}$、$t_k$ は方位 $k$ の
レイが境界に達する距離。

内部状態は入力に**混ぜない**（知覚は純粋幾何）。内部状態の役割はどの場をリクルートするかの
ゲートのみ（C.7）。全行動が同一の方位基盤を読むことで、1 重み集合への多重化が構造的要求に
なり、壁も知覚可能になる。旧 6D vector 符号は教師をほぼ線形にする（§4 の自明性批判）ため
レガシー専用。

### C.4 教師ベクトル場

最近傍への**生の**相対ベクトル $r_c$ を用い

$$z = \alpha_f r_{\text{food}} - \alpha_t r_{\text{threat}} + \alpha_s r_{\text{shelter}},\qquad
v = \tanh(\gamma\|z\|)\, z/(\|z\|+\epsilon),\quad \gamma=2$$

生ベクトルゆえ遠方で $\tanh$ 飽和＝全速、接近で減速（到着減速則が自動で出る）。
壁整合項: 境界帯（幅 $m=0.15$）で外向き成分を smoothstep で $-\kappa$ 倍（$\kappa=1$）に反転。
純減衰 $\kappa=0$ は粘性帯となり壁滞在が悪化する。

状態別駆動重み（ほぼ one-hot; `--alpha-mix` で競合を強められる、§4 参照）:
$\alpha_{\text{food}}=(1.80,.03,.03)$, $\alpha_{\text{threat}}=(.03,2.20,.03)$, $\alpha_{\text{shelter}}=(.03,.03,1.80)$。

### C.5 神経修飾場

$H=144$ ユニットを仮想シート $[0,1]^2$ に配置（シートは**制御された重なりの生成装置**、
解剖学的主張ではない）。カテゴリ中心は半径 $\rho=0.28$ の円環上に等間隔（全ペア等重なり）。

$$\sigma_k^c = \sigma_0\, \iota_k^c\, \mathbb{1}[\iota_k^c \ge \theta_{\text{cut}}],\qquad
\iota_k^c = e^{-\|u_k-\bar c\|^2/2s^2},\quad \sigma_0=0.8,\ s=0.22,\ \theta_{\text{cut}}=0.15$$

混合場 $\sigma(w) = \sum_c w_c \sigma^c$。参加度は $\sigma$ 閾値でなく交差率 $\nu_k$ の台で測る
（ゲージ不変性、§5.3）。$\sigma_k=0$ の真の沈黙は第 1 隠れ層のみ（$\sigma=0$ リーク、§5）。

### C.6 モデル — NNN sample 水準（標準）

構造 $[32,144,144,2]$、$T=64$、$h=0.2$。第 1 線形層後に $T$ 本複製、各隠れ層で
$\tilde a_t = a_t + \varepsilon_t$, $\varepsilon_t \sim \mathcal N(0,\mathrm{diag}(\sigma)^2)$、
交差活性 $z_t = \frac12(|b^+_{t\oplus1}-b^+_t| + |b^-_{t\oplus1}-b^-_t|)$
（$b^\pm_t = \mathbb 1[\tilde a_t \gtrless \pm h]$、$T$ 方向巡回 XOR）。逆伝播は幅 $2h$ 一様
カーネルの密度推定を $T$ 平均した低分散勾配。読み出し前に $T$ 平均。

**sample が標準である理由**: analytic（平均場）には閾値下障壁が無く SR が存在しない（§6）。
計算量は約 65 倍（実測 47 vs 0.7 ms/step、バッチ 512）で GPU が事実上の前提
（40k ステップ: RTX 5080 で約 1.5 分、CPU 約 31 分）。

### C.7 学習（blended、標準）

閉ループは純粋状態を走らないので、**実行時分布を直接学習**する。各ステップで独立に:
混合 $w$（0.3 純粋頂点 / 0.5 目標⇔脅威辺 / 0.2 Dirichlet(1,1,1)）、脅威再配置（内箱一様・
keepout 充足）、食物マスク（各 0.2 で欠損; 感覚と教師で同一）、位置ミニバッチ（一様プール
16384 から 512）。損失 $\|f_\theta(x;\sigma(w)) - v(\cdot;\alpha(w))\|^2$、Adam 3e-4、40k step。
純粋 3 頂点学習は混合場で素朴出力混合に劣る（sector では破綻; §7.12b）。
**注意**: blended 学習は混合を見ているため §7.11 のゼロショット補間主張には使えない
（あの主張は pure 学習ネットに依拠）。

### C.8 閉ループ

フレーム手順: 脅威移動 → 時計 → 内部状態 → 調停 $w$ → 場 $\sigma(w)$ → 感覚 → ネット →
運動積分 → 摂食。

**(a) 概日時計（標準の goal 決定）**: 周期 $P=420$、昼比率 $D=0.62$（昼 260 / 夜 159 フレーム）。
$\varphi < D$ で goal=food、$\varphi \ge D$ で goal=shelter。**内部状態を一切参照しない**——
これが恒常性でなく概日である定義。夜長は睡眠でなく**帰巣移動を含む**必要があり、$P/D$ は
帰巣成功率が律速（420/0.62 で 0.96、420/0.55 で 0.75。※dataclass 既定はインポート時束縛
なので周期を振る実験は必ず引数で渡す）。

**(b) 内部状態**: hunger は 0.006/フレームで増加、摂食で −0.6。休息: goal=shelter ∧ 到着半径
0.12 内で 50 フレームの静止カウントダウン（時間ベース——「巣内でのみ減衰」は脅威の一押しで
永久停滞するため）。夜間は再点火し実質朝まで眠る。

**(c) 調停**: 脅威距離 $d_t$ に対し $a_0 = \mathrm{clip}(G e^{-(d_t/R_{\text{th}})^2}, 0, 1)$、
$G=1.7$, $R_{\text{th}}=0.50$。3 つの補正:

- **panic 拒否**（$R_p=0.22$）: $\text{keep} = 1 - e^{-(d_t/R_p)^2}$ を以下 2 減衰に乗じる。
  至近距離では減衰そのものが消え、回避が決して切れない。
- **refuge**（goal=shelter、$R_r=0.30$）: $a \mathrel{*}= 1 - \text{keep}\cdot e^{-(d_s/R_r)^2}$。
  入口で徘徊脅威に足止めされず駆け込む。
- **勇気ゲート**（goal=food、$\rho=0.9$、onset 0.6）: hunger の smoothstep で $a$ を最大 $\rho$
  減衰。飢餓と被食リスクのトレードオフ。

$w^{\text{tgt}} = (1-a)e_{\text{goal}} + a\,e_{\text{threat}}$、$w \leftarrow (1-\beta)w + \beta w^{\text{tgt}}$
（$\beta=0.20$; 低域通過がチャタリングを防ぎ、リクルートを濃度の重なりのように混ぜる）。
表示ラベルは $\arg\max_c w_c$（goal ではない）。

**生物規範との対応（§2 の慎重さで——主張ではなく解釈の錨）**: 概日 goal は SCN 系の遅い
調停、勇気ゲートは risk-sensitive foraging（飢餓が被食リスク許容を上げる）、panic は
defensive distance（至近脅威で逃走が全てに優先）、refuge は避難所指向逃走、$w$ の低域通過は
容積伝達的な濃度動態（§2.7）に対応する。いずれも「場が行動カテゴリの鍵」という結びつけ
（§4 で自認した弱点）を強めるものではなく、調停の各機構が既知の行動学的原理と 1 対 1 で
読めることを保証するための設計制約である。

**(d) 運動積分（heading、標準）**: 速さとヘディングを**分離して**平滑化。
$s \leftarrow (1-\mu)s + \mu\|\hat v\|$（$\mu=0.2$）、ヘディングは目標方向へ最大
$\Omega=0.65$ rad/フレームで旋回、$\Delta p = \mathrm{d}t\, G_s \min(1, s/s_{\text{ref}})\hat h$
（$\mathrm{d}t=0.04$, $G_s=0.9$）。**速度ベクトルの低域通過は禁止**: 相反指令を相殺し、健全な
ネットの前で個体が凍る（実測: 停止フレームの $\|\hat v\|$ 中央値 0.116 vs 平滑後 0.032、35% が
反転。これが「無意味な停止」の正体）。$\|\hat v\|\to0$＝凍結という行動 SR の意味は magnitude
チャネルに残る。$s_{\text{ref}}$ は学習後に自動測定（全域・実行時混合の $\|\hat v\|$ の
30 パーセンタイル; 中央値だと半分の時間で全速に達せず徘徊脅威に詰められる）。
境界は位置射影＋外向き速度反転（$e=0.5$）だが現行速度では発火しない（12000 フレームで 0 回;
保険として保持）。

### C.9 指標

`world.rollout`: `contact_frac`（$d_t<0.08$）/ `wall_frac`・`wall_run`（境界帯 0.04）/
`stall_frac`・`stall_run`（休息・巣内・食物到着中を除く $s<0.05$）/ `foods_per_1k` /
`shelter_frac` / `d_threat_min` / `close_frac` / 移動量。
診断側: **帰巣成功率**（夜内に到着半径へ入れた夜の割合）、**食性・巣利用の均等度**
（正規化シャノンエントロピー）。ベンチマーク 3 スコア（separation / signal / task_err）は
`protocol.capability`。ノイズ掃引ではゲージ不変比 $h/\sigma$ を必ず併記（§5.1）。

**参照挙動**（標準設定、4 ネット × 8 エピソード × 1680 フレーム）:
stall 0 / wall 0.001 / contact 0.0009 / 帰巣 0.86 / 食性均等 0.86 $[119,24,105]$ /
巣均等 0.94 $[39,71]$ / foods_per_1k 4.6。

### C.10 標準パラメータと実装規約

**唯一の規範は `world.LoopParams` の既定値**。ドライバ argparse は LoopParams から既定を
読み（`_LP = world.LoopParams()`）、`viz.animate` は LoopParams を直接受け取る。ヘッドレス
実験（`world.rollout`）と画面のアニメーションが同一オブジェクトを取るため、**測った動物と
見ている動物が一致する**。感覚の module 既定は sector（レガシー 5 スクリプトは
`set_sensing("vector")` を冒頭で明示）。

| 分類 | 値 |
|---|---|
| モデル | sample, $T=64$, $h=0.2$, 構造 $[32,144,144,2]$, device auto |
| 場 | $\sigma_0=0.8$, $\rho=0.28$, $s=0.22$, $\theta_{\text{cut}}=0.15$ |
| 教師 | $\gamma=2$, $m=0.15$, $\kappa=1$ |
| 学習 | blended 40k / 512 / 3e-4, プール 16384 |
| 概日 | $P=420$, $D=0.62$ |
| 調停 | $R_{\text{th}}=0.50$, $G=1.7$, $\beta=0.20$, $R_r=0.30$, $\rho_{\text{勇気}}=0.9$, $R_p=0.22$ |
| 運動 | heading, $\Omega=0.65$, $G_s=0.9$, $\mathrm{d}t=0.04$, $\mu=0.2$, $s_{\text{ref}}$ 自動 |
| 生態 | $R_{\text{eat}}=0.10$, 到着 0.12, $N_{\text{regrow}}=260$, hunger 0.006/−0.6, 休息 50 |
| 脅威 | 速さ 0.01, 内箱 0.72, keepout 0.45 |

**削除済みレガシー**（2026-08-08、クリスプさ優先で撤去。git 履歴にのみ残る）:
満腹則 `--goal-mode drive`（shelter_need / HUNGER_LO / SHELTER_LO ごと）、
vector 積分 `--integrator vector`（停止アーティファクトの原因）、
`--speed-mode cruise`（SR の崩壊を隠す表示）、`--layout random` と個数フラグ
（C.2 の制約を満たさず概日帰巣が保証されない）。**唯一残るレガシーは vector 感覚**で、
旧記録を再現するレガシー 5 スクリプトが `set_sensing("vector")` で明示ピン留めする。

**既知の限界**: (i) 脅威接触は $10^{-3}$ オーダーで残る（$\rho_{\text{勇気}}=0.6, R_p=0.34$ で
測定上 0 になるが採餌約 2 割減）。(ii) 帰巣 0.86 は散らばりとの交換（C.2）。
(iii) blended 学習はゼロショット補間主張に使えない（C.7）。
